// GENERATED by scripts/embed_agent_guide.py — do not edit.
//
// docs/AGENT-GUIDE.md, compiled into the binary so the `logician://guide`
// resource works from an installed binary with no repo and no side-car bundle
// around it. Re-run the script after editing the guide; MCPResourceTests fails
// the build if the two ever disagree.

/// docs/AGENT-GUIDE.md, verbatim.
let agentGuideMarkdown = #"""
# Logician Agent Guide

**This document is written for the AI agent using Logician.** Read the Core concepts and Workflows sections before your first tool call; consult the Tool reference as needed. Everything here was empirically verified against Logic Pro 12.

Logician controls Logic Pro through its **data plane**: the Mackie Control surface protocol over virtual MIDI (with Logic's own LCD/LED/fader echoes as verification), MIDI-note-bound key commands, timed MIDI streams for recording, and element-addressed macOS Accessibility for reading structure. **No coordinate-driven UI scripting.** A few verified fallbacks synthesize one of Logic's own shortcuts or press one hit-tested element, and each names its route in the result's `write_route` — trust that field rather than an assumption about how a write happened. (Two tools also offer an explicit `allow_mouse: true` fallback that briefly takes the pointer; it is off by default.)

## First call, always

Run `logic_health` first. It starts the bridge daemon, checks every setup requirement, and returns a concrete fix string for anything missing. Requirements: Logic Pro running with a project open, Accessibility granted, a Mackie Control device in Logic bound to the "Logic MCP MCU" ports (one-time manual setup), and learned key commands (learned automatically on first use — the Key Commands window flashes briefly; run `logic_setup_key_commands` once to do all learning up front instead).

## Core concepts

**1. Compare-and-set discipline.** *Some* write tools accept a compare-and-set argument — `expected_current_value`, `expected_current_db`, `expected_current_position`, `expected_current_bpm`, `expected_current_name`, `expected_current_bypassed`, or a whole `expected_current` object where several slots are written at once. **Which one, and whether there is one at all, is per tool: the schema is the answer**, and an argument a tool does not declare is refused (`invalid_arguments`), never silently ignored. Pass it whenever you know the current value: the tool reads the control, REFUSES with `precondition_failed` and writes nothing if reality disagrees, then converges, reads Logic's echo back and reports `before`/`after`. The writes with no such argument still verify by readback — they just cannot tell "the value was already wrong" from "you asked for this". Read before you write either way, and remember every value argument is ABSOLUTE (the single exception is `logic_set_track_volume`'s `relative_db`).

**2. Everything is verified — trust the report.** `verified: true` means Logic's own feedback (LCD echo, motorized fader, arrangement map, document flags) confirmed the result. `verified: false` or `success: false` with an `error` string means exactly what it says; nothing is silently retried into a lie. Two refinements live in "Reading a result" below and are worth reading before you build a loop on these fields: `success` can be `false` while the MCP result is not an error, and a handful of tools give `verified` a specific, narrower meaning that their own description states.

**3. Track names: use what `logic_list_tracks` shows — and read its `partial` field.** Tools take the full Accessibility names ("Lofi Pad"), not the MCU's 7-char truncations ("LofPad"). Duplicate names: pass `track_number` too. **The list can be incomplete**: Accessibility publishes only the track headers Logic has RENDERED (19 of 25 strips on the reference project). The result therefore carries `partial`, `partial_evidence` (headers scrolled out above, gaps in the numbering, collapsed stacks, a scrollable Tracks area), `missing_track_numbers` where the numbering names them, and `completeness` — which is `"partial"` or `"unknown"` and never `"complete"`, because a row Logic has not rendered publishes nothing at all. `partial: false` means nothing proved rows missing; it is not a census. Expand stacks with `logic_set_track_stack`, and remember that headerless strips are never in this list at all (3b).

  **`logic_list_tracks` is a PARTIAL view and looks complete.** It returns only the track headers Logic has currently rendered — 19 of the reference project's 25 strips — with a plain `success: true` and nothing saying what it could not see. Use **`logic_list_strips`** for the census: it walks the control surface, which reaches every strip in the project regardless of scrolling, and marks any strip it cannot tie to a rendered header as `unresolved` instead of omitting it.

**3b. The master chain and the buses are strips too — address them by name.** `Stereo Out`, `Master`, `Aux 1`, bus channels: `logic_list_tracks` does NOT list them (they have no track header in the Tracks area), but the mixing, send and plugin tools take their names anyway. Pass the name Logic shows in the Mixer (`"Stereo Out"`), never the 6-character LCD abbreviation (`St Out`). What differs from a track:

  - **Which plane resolves them.** A track is selected through Accessibility; a headerless strip is resolved and selected on the control surface, so those calls need the MCU bridge. Results say which was used in `selection_route` (`ax_track_header` / `mcu_channel`). Addressing a headerless strip moves Logic's focused CHANNEL while the previously selected track's header stays selected, and the surface's plugin and send views follow the channel — so a later call on that track detects the divergence and reselects the track for real before reading anything; `selection_readback_route: "realigned_ax_reselect"` in the result says that happened. A divergence that cannot be realigned is refused with both strips named.
  - **`track_number` is for tracks only.** Passing one pins the call to the track-header plane, so a number plus an output name is an error, not a reroute.
  - **The Accessibility-only tools need the strip on screen.** `logic_survey_plugins`, `logic_open_plugin`, `logic_plugin_preset` and `logic_set_insert_bypass` read an *inspector* strip, and an inspector only shows the selected track's own strip and its output. `Stereo Out` is usually reachable that way; `Master` and the auxes usually are not. **Opening the Mixer does NOT fix that** — the experiment ran on 2026-08-28 and the answer is no: the Mixer window publishes every strip (`logic_set_mixer` reports them as `mixer_strips`, `Master` and the auxes included), but they are not inspector strips and an Accessibility read of `Master` fails with the Mixer open exactly as it does with it closed. Worse, the Mixer is a second document window carrying the same project and can shadow the project window while Logic is in the background. **For `Master`, an aux or a bus take a control-surface route** — `logic_list_inserts {route: "mcu"}`, `logic_list_plugin_parameters {track_name, insert_slot}`, the `logic_mcu_*` tools — none of which need a window.
  - **Insert numbering can differ between the planes, and on an output it was observed REVERSED**: on `Stereo Out`, Accessibility listed `Sensor, Limiter, Channel EQ` while MCU slots 1-3 read `Channel EQ, Limiter, Sensor`. Never translate an AX `insert_index` into an MCU `insert_slot`; list with the route you are about to write through, and read `route_used` in the result to know which numbering you were given.
  - **Automation is the one thing a headerless strip cannot have written.** `logic_record_automation` switches the track to Latch and verifies that switch through the *inspector* strip's automation-mode label, which a strip with no track header does not publish — so the mode change cannot be confirmed and the call fails there. `logic_read_automation` still works (it samples Logic's own echo over the surface), but it reports `automation_mode: null` on such a strip, because the label it would read is the same one. Automate the tracks feeding a bus, not the bus.
  - **Ambiguity refuses.** Two strips whose names abbreviate to the same six LCD characters produce `ambiguous` with the cells listed, and nothing is written.

**4. Bars and beats are 1-based; end bars are exclusive.** `start_bar: 5, end_bar: 9` = bars 5–8 (the range ends where bar 9 begins). Beats accept fractions (`beat: 2.5` = the off-beat after 2). Bar 1 = project start. Both **tempo changes and meter changes are followed**: the tempo map is read out of the Tempo List and the meter map out of the Signature List, and both are integrated (see 4b). Read the current tempo and signature with `logic_get_transport`, the whole meter map with `logic_list_signatures`.

**4b. Tempo maps are read and integrated; the fallback still warns.** Two families of tools take bars:

  - **Logic interprets the bars** — `logic_bounce_range`, `logic_evaluate_change` methods `bounce`/`solo_bounce`, `logic_set_cycle_range`, the region tools. Correct under any tempo map, always. Nothing to read, nothing to warn about.
  - **Logician converts bars to seconds itself** — `logic_render_track`'s bar-range slice, `logic_evaluate_change` method `render`, `logic_record_midi`'s note placement and verification slice, `logic_record_automation`'s point placement. These now read the project's **tempo map** out of Logic's Tempo List (`View > List Editors > Tempo`: one row per tempo event, position + BPM) and **integrate it piecewise** — exact for step tempo changes, and reported back in a `tempo_map` block. The read costs ~2 s, moves **no** playhead, and is cached per project (invalidated by `logic_set_tempo`, by any recording whose Smart Tempo mode was not verifiably Keep, and whenever the control bar reports a tempo the cached map cannot account for — so editing a tempo in Logic between calls does not leave a stale map behind).

**The meter map works the same way, one tab over.** Time signatures are read out of the Signature List (`View > List Editors > Signature`, or list them yourself with `logic_list_signatures`), and a project whose bars are not all the same length has that integrated too: bar→beats stops being one multiplication and becomes the sum of each bar's own length. Bar lengths count QUARTER notes, which is what Logic's BPM counts — 6/8 is three beats a bar, 7/8 three and a half. Two rules worth knowing: a meter map with only ONE bar length is reported and deliberately not used (so a constant-meter project's boundaries are exactly what they have always been, and your `beats_per_bar` override still applies), and a VARYING map overrides `beats_per_bar` entirely — the project's own grid wins. Every affected result carries a `meter_map` block, including when the Signature List could not be read, which is the case where the old constant-meter assumption is still live.

One honest limit remains on the tempo read. **Tempo curves** (two points joined by a continuous ramp) are not distinguishable in the Tempo List — it publishes only Position, Tempo and SMPTE Position — so a curve is integrated as a step; when a range could be affected the `warning` says by how many ms.

When the Tempo List cannot be read at all, the pre-map behavior is the fallback: the tool parks the playhead at the range's first bar, reads the control-bar tempo, parks at the last bar, reads again, and restores (~0.13 s per bar of travel). Differing readings then give a `warning` naming both, or a `precondition_failed` refusal where a warning would not be enough (`method: "render"`, and `speed > 1` on `logic_record_midi`). Two agreeing readings are evidence, not proof: a map that returns to its old value between the sample points reads as constant.

**5. Single-project mode.** Opening/creating a project closes the current one. If the current project has unsaved changes you MUST pass `if_current_modified: "save"` or `"dont_save"` — an explicit decision, otherwise the call refuses. Nothing ever saves except `logic_save_project` (and lifecycle calls where you explicitly chose saving).

**6. MCU physical insert slots ≠ Accessibility ordinals.** The control-surface route takes `insert_slot` (1–8, the Mackie physical slot). List them with `logic_list_inserts {route: "mcu"}` first; empty slots show `--`. The Accessibility route of the same tool returns `index` instead, which is what `logic_open_plugin` and `logic_set_insert_bypass` take. `logic_remove_plugin` takes both, one per route: `insert_slot` on its default mouse-free route (needed when the same plugin occupies several slots), `insert_index` only for the `allow_mouse` Accessibility fallback. `route_used` in the result says which you got.

**7. Destructive operations have guards but Undo is your friend.** `logic_delete_region` does not assume the selection is exclusive, it makes it so: Logic's own project-wide `Deselect All` clears everything and is proven to have landed before the one named region is selected back, because Delete takes every selected region in the project while the arrangement map only shows the rows Logic has rendered. Where that command is unavailable and rows are provably hidden, it refuses and names them rather than deleting blind. `logic_delete_track` re-verifies the selection immediately before firing. Both are restorable with `logic_trigger_key_command {name: "Undo"}` — but ONLY fire Undo right after a known edit (the menu shows no operation name; a blind Undo can revert something else).

**8. Values follow the control's own units.** Volume/sends in dB; pan −64..+63 (0 center); plugin parameters in whatever the LCD shows (read them first). New sends start at −∞ dB and are inaudible — pass `level_db` to `logic_add_send` so the send is created AND levelled in one call. Numeric convergence lands within the control's step size (typically ±0.1 dB, ratios ±0.1).

**9. Real time is real.** MIDI recording and automation recording play through the actual timeline (bars × beats × 60/BPM seconds, roughly doubled with verification). `logic_record_midi` accepts `speed: 2..8` to record at raised tempo (auto-restored) when you don't need to hear the take — still refused on a non-constant tempo, because restoring one BPM cannot restore a map (that refusal does not soften with a readable map; the map tells us the take's timing, not how to put a slider write back). Renders/bounces are offline and fast, and near-constant in length: a master bounce ~6–7 s, a single-track freeze render ~9–13 s (measured on a 19-track project).

**10. One call beats three, and warm paths are faster still.** `logic_set_plugin_parameter {track_name, plugin_name, parameter, target_value}` finds the insert itself — there is no need to call `logic_list_inserts` for a slot number first, and the result says which slot it used in `resolved_slot`. Measured on the reference project: that write lands in ~0.9 s cold and ~0.5 s when it follows another call on the same plugin, against ~15.8 s for the list-then-read-then-write chain it replaces. Two habits keep it there: batch your parameter work per plugin (consecutive calls on the same track+slot skip all setup), and read a plugin's parameters once with `logic_list_plugin_parameters` rather than per write — the names are cached per project, and the first uncached read of a plugin is the one slow call (~3.4 s, more for a big instrument).

**11. The tool list can be narrowed at launch.** `--toolsets=core,delivery` (or `LOGICIAN_TOOLSETS`) makes the server advertise only the sets a session needs — `core`, `regions`, `composition`, `delivery`, `project`, `keycommands`, or `all`, which is the default. A whole surface is 84 tools; `core` alone is 42 and costs less than half the bytes. Nothing about a tool changes when it is offered; a tool that is NOT offered answers a call with the unknown-tool error plus a line naming the sets that hold it and the flag that would bring it back. Nothing an agent does can change the active sets — it is the user's launch configuration.

**12. The surface is built for client-side tool search, and `--toolsets` is the floor for clients without it.** Claude Code (2.1.221+) defers every MCP tool definition by transport, stdio included: it hands the model the 84 tool NAMES and the server instructions, and a schema arrives only when the model searches for one. Nothing is flattened — the typed schema you get back is the whole schema — so the cost of the surface is paid on demand instead of up front, and the names plus the six group names in the server instructions are what you navigate by. Two caveats worth knowing. **Behind a proxy, tool search is off**: Claude Code disables it whenever `ANTHROPIC_BASE_URL` points at any host other than `api.anthropic.com`, unless `ENABLE_TOOL_SEARCH` is set explicitly, so a proxied user pays for all 84 definitions in every prompt — that is the case for `--toolsets`. **Some clients cap the list instead of searching it** (Cursor truncates around 40, VS Code shares 128 with extensions, Windsurf 100); there `--toolsets` is not an optimisation but the difference between a working session and a silently truncated one.

**13. `logic_find_tool` is the search for everywhere else.** Where your client has no tool search of its own — behind a proxy, under a hard cap, or launched with `--toolsets=core` — this is how you get from an intention to a callable schema. Give it a few technical words (`{query: "record a volume automation pass"}`) and it returns the top 5 (up to 10) matches, each with the FULL typed definition: name, title, description, complete `inputSchema` and the safety annotations. It is a keyword search — Okapi BM25 over names, descriptions, argument names, argument descriptions and enum values — so name the thing rather than describing the outcome: `quantize swing region` lands in one hop, `what is in this project` cannot, because one content word does not discriminate between forty tools that mention a project. An exact tool name (with or without the `logic_` prefix) is answered with that tool first, which is why there is no separate schema-lookup tool: find IS the lookup. Three things worth knowing. It searches the WHOLE registry however the session was narrowed, and every hit says which toolsets hold it, whether it is `active` here, and — when it is not — the flag that would offer it, so a narrowed session can tell "no such tool" apart from "not switched on". `schemas: false` returns names, titles, descriptions and toolsets only, about half the bytes, for a cheap first pass before you ask for the real thing. And it is the one tool here that never touches Logic Pro at all: it reads a constant, it is in every toolset, and it works with the app closed.

## Workflows (recipes)

**Understand a project:**
`logic_health` → **`logic_list_strips`** (the complete census — every strip including the ones `logic_list_tracks` cannot see) → `logic_list_regions` (the arrangement map: every region's name, bars, type) → `logic_get_transport` (tempo/meter) → `logic_markers {action: "list"}` for the song's sections → per interesting track: `logic_list_inserts` + `logic_mcu_sends`. `logic_list_tracks` is still the right call when you need the *rendered* rows specifically (selection state, stacks, expansion — and **check `partial`**: if it is true, the project has tracks you cannot see), but it is a partial view of the project and `logic_list_strips` is the complete one.

**Read a mix you did not build:**
**`logic_mixer_snapshot`** — one call gives every strip's dB, mute, solo, selection, record-arm and pan, so you can plan before touching anything instead of making one call per track per property. Pair it with `logic_read_automation` on any track whose balance looks deliberate: automation is invisible in a snapshot, and `logic_record_automation` OVERWRITES the range it rides. Read the lane, then decide.

**Compose a part:**
`logic_create_track {type: "software_instrument"}` → **`logic_load_instrument {track_name, instrument}`** (the create result names the new track as `created_track {track_number, track_name}` — use that `track_name`, no `logic_list_tracks` in between; the instrument slot is a different mechanism from `logic_add_plugin`, which fills an *insert*; a new track without this step usually makes no sound) → `logic_record_midi {track_name, notes: [{pitch: "C3", bar: 5, beat: 1, duration_beats: 1, velocity: 100}, ...], cc_events: [...], pitch_bends: [...]}`. Verification renders the recorded bars and returns metrics + a listenable WAV path. Notes: pitch as MIDI number or name (Logic convention: C3 = 60). start_bar must be ≥ 2 (one pre-roll bar). This plays in REAL TIME, so a 32-bar part costs a minute of wall clock — for anything long, or for several parts at once, see the next recipe.

**Compose a whole arrangement in one call:**
**`logic_import_midi {at_bar, tracks: [{name, channel, notes: [...]}, ...]}`** writes the whole thing as a Standard MIDI File and lets Logic import it: bass, keys and a pad arrive together in about ten seconds regardless of how long the music is. Same note vocabulary as `logic_record_midi`, widened to many tracks. Three things to know. **Compose onto the user's OWN tracks with `to_track`** — per `tracks[]` entry, not per call, so one import can send the bass line onto `Bas` and the kit onto `Drums` while a third entry keeps a new track of its own. The imported material then plays through the destination track's existing instrument, which is the reason to want it: Logic's importer can only ever make new tracks with default patches, and a routed entry escapes that. It costs a measured 5.9 s per routed track — the import still lands on a temp track, the region is moved onto the destination, and the emptied temp track is deleted — and destinations are checked before the import runs, so a name that does not exist (or an output/aux/bus strip, which has no lane for a region) is refused with the project untouched. For an UNROUTED entry, Logic names the new TRACK after whichever default patch it loads, so your name comes back on the REGION — `logic_rename_track` afterwards if the track name matters, and `logic_load_instrument` to put the right sound on it. And it answers Logic's "Also import tempo information?" with **No** unless you ask otherwise, so the project's tempo map is untouched. Prefer `logic_record_midi` when the take should be audible as it happens; prefer this for everything else, including anything longer than a few bars.

**Track a human (a singer, a guitarist):**
`logic_create_track {type: "audio"}` → `logic_set_metronome {enabled: true}` if they want a click → **`logic_set_track_record_arm {track_name, enabled: true}`** → tell the user you are rolling → `logic_set_playhead {bar}` → `logic_set_playing {playing: true}` → …the take happens in real time… → `logic_set_playing {playing: false}` → **`logic_set_track_record_arm {enabled: false}`**. Two things to know before you roll: Logic lets several tracks be armed at once and arming one does not disarm another, so check `logic_mixer_snapshot`'s `record_armed` column first; and input assignment and input monitoring are still not reachable from this server, so the user has to set the mic input and monitoring by hand. Always disarm afterwards — an armed track left behind records the next time anyone presses record.

**Shape a sound:**
`logic_set_plugin_parameter {track_name, plugin_name, parameter, target_value}` is the whole move when you know the parameter — it resolves the insert itself and reports it as `resolved_slot`. Otherwise: `logic_add_plugin {track_name, plugin_name}` (mouse-free via the control-surface browser; exact catalog names like "Compressor", "Channel EQ") → `logic_list_plugin_parameters {track_name, insert_slot}` to see the names (page-capped; pass `max_pages` for giants; `logic_list_inserts {track_name, route: "mcu"}` gives the slot numbers, and is also how you tell two copies of one plugin apart) → `logic_set_plugin_parameter` per change → or browse factory settings with `logic_plugin_preset` (`action: "list"` to see the names, `action: "select"` with `name` to load one).

**Mix moves:**
`logic_set_track_volume {db}` / `logic_set_track_pan {position}` / mute/solo `{enabled}` / `logic_add_send {destination: "Bus 1", level_db: -12}` (one call: a send without a level is silent) / `logic_mcu_set_send {send, level_db}` to change one afterwards / `logic_remove_send {send, destination}` to take one out again — read-first, verified, and never a blind Undo. **`db` and `position` are ABSOLUTE**, so "2 dB louder" is either a read followed by a write, or — better — `logic_set_track_volume {track_name, relative_db: 2}`, which measures the offset from the value it reads immediately before moving the fader. Pass `expected_current_db` / `expected_current_position` / `expected_current` (mute and solo) whenever you already believe you know the current value: a stale belief then becomes a `precondition_failed` refusal instead of a wrong write.

**Hear what a plugin is doing (the cheap A/B):**
`logic_set_insert_bypass {track_name, plugin_name, bypassed: true}` → bounce or render → set it back. Seconds, against `logic_evaluate_change`'s 30–50 — use `evaluate_change` when you want the two versions measured and carried back as audio, and bypass when you just want to hear the difference.

**Read what is already there:**
`logic_select_region {track_name, start_bar}` → `logic_list_events` for the region's actual notes (position, pitch, velocity, length). The Event List shows the SELECTED region only — an empty result means nothing is selected, not that the project has no MIDI. `logic_list_events` will do the selecting for you if you pass `track_name`.

**Fix one flubbed note without re-recording:**
`logic_list_events {track_name, start_bar}` to see the region's notes → **`logic_edit_event {action: "set", bar, beat, pitch: "D#2", new_pitch: "E2"}`** (or `velocity`, `length`, `to_bar`/`to_beat`). Address a note by its position AND its pitch — a chord publishes several rows on one position and an ambiguous address is refused, not guessed. `logic_record_midi` is the composer; this is the scalpel for one wrong note in a take worth keeping. Also `action: "delete"` for a note that should not be there, and `action: "create"` for one that should — inside the region's own bars, because Logic will happily put a note past the region's edge where it exists and never sounds.

**Mark the map:**
`logic_markers {action: "create", bar: 33, name: "drop"}` → `logic_markers {action: "list"}` → `logic_markers {action: "goto", name: "drop"}`. Markers are created at the playhead, and Logic's position stepping lands inside the bar rather than exactly on its line — read the bar/beat back. Logic also renumbers its own default marker names by position, so address markers by `bar` when identity matters.

**Work on the master chain (or a bus):**
Use the strip name directly — `logic_list_inserts {track_name: "Stereo Out", route: "mcu"}` to see the MCU slots → `logic_list_plugin_parameters {track_name: "Stereo Out", insert_slot}` → `logic_set_plugin_parameter {track_name: "Stereo Out", insert_slot, parameter, target_value}`. `logic_list_tracks` will not mention `Stereo Out`; that is expected (see concept 3b). A/B a master change with `logic_evaluate_change` method `bounce`, which captures the whole mix and needs no solo.

**Automate:**
**Read the lane first** — `logic_read_automation {track_name, parameter, start_bar, end_bar}` samples what is already there, which matters because the write below overwrites the whole range it rides (that is why it is flagged `destructive`). A flat result is ambiguous by nature: an unautomated lane and a genuinely flat curve look the same from a playhead chase, and the result says so. Then:
`logic_record_automation {track_name, parameter: "volume"|"pan"|"send"|"plugin", points: [{bar, beat?, value}], ramp: true}` — for sends add `send: N`, for plugins add `insert_slot` + `plugin_parameter`. Values: dB for volume/send, −64..63 for pan, the parameter's own units for plugins. Verification replays or playhead-chases each point and reports expected vs observed. First point needs bar ≥ 2.

**Edit the arrangement:**
`logic_list_regions` → `logic_select_region {track_name, start_bar}` → `logic_move_region {by_bars, by_beats}` / `logic_copy_region {to_bar, to_track?, move?}` / `logic_delete_region` / `logic_split_region {at_bar}`. A region's own parameters — quantize, transpose, velocity, loop, mute, and on audio regions gain, fine tune, fades and reverse — live in the Region inspector and are read with `logic_get_region_params` and written with `logic_set_region_params` (see "Tighten a sloppy take" and "Fade a clicking edit" below); `logic_rename_region` writes the same panel's name field. Split is one call, not the old three-step recipe — it parks the playhead EXACTLY (see caution below), checks that the point is inside the region, fires the command and proves the result against the arrangement map.

**Edit many regions at once:**
`logic_select_regions {mode, track_name, start_bar}` extends a selection the way Logic does — `track` (the whole track), `following` (everything after the anchor, all tracks), `following_same_track`, `all`, `none` — and reports how many regions ended up selected. Then fire ONE edit across all of them (`logic_trigger_key_command {name: "Delete"}`, a nudge, `Cut`). This is the difference between one call and a thousand round trips on a podcast edit. The count only sees VISIBLE track rows; the selection itself is project-wide, so an edit can reach more than the number you were shown.

**Tighten a sloppy take (quantize with feel):**
`logic_get_region_params {track_name, start_bar}` to see what the region already carries → `logic_set_region_params {track_name, start_bar, quantize: "1/16 Note", q_swing: 58, q_strength: 80}` — one call, because the tool writes quantize FIRST (Logic greys every Q-row out while Quantize is Off, so a separate swing call would land on a dead control). `q_strength` below 100 is the "leave some feel" knob: it pulls notes part of the way to the grid instead of all the way. Nothing is destroyed — these are Logic's playback parameters, the recorded notes are untouched (`logic_list_events` keeps showing where they were actually played), and `quantize: "Off"` puts the take back exactly as it was. Then bounce and listen: a grid is a decision, not a fix.

**Same move across a whole track:** `logic_select_regions {mode: "track", track_name, start_bar}` → `logic_set_region_params {scope: "selection", quantize: "1/16 Note"}`. Only `quantize`, `loop` and `mute` work that way — over a multi-selection Logic turns every NUMERIC region control into a relative one and the tool refuses them by name rather than writing something it cannot verify.

**Fade a clicking edit, gain-stage a hot region (audio):**
An edit that clicks is a waveform cut at a non-zero sample, and the fix is a fade of a few milliseconds, not a redo of the edit: `logic_set_region_params {track_name, start_bar, fade_in_ms: 8, fade_out_ms: 8}`. For a musical fade, give it a shape — `{fade_out_ms: 400, fade_out_curve: -40}` — and where two audio regions butt up against each other, `fade_type: "EqP (Equal Power Crossfade)"` is the one that keeps the level constant through the join. A region that is simply too loud (or the one quiet take in a comp) is `gain_db`: **decibels**, non-destructive, and reversible with `gain_db: 0` — reach for it before an automation pass, and leave the fader for the mix. `reverse: true` flips an audio region for a reverse-cymbal lift without touching the file. Read first with `logic_get_region_params`, then prove it with your ears: `logic_render_track {track_name, start_bar, end_bar}` before and after gives you peak/RMS on the same slice.

**Strip the silence out of a speech take:**
`logic_remove_silence {track_name, start_bar}` first — with `apply: false` (the default) it opens Logic's Remove Silence window, reads its LIVE preview ("this would leave 9 regions") and closes it again, changing nothing. If the number looks right, call again with `apply: true`.

**Resample — print audio back INTO the project:**
`logic_bounce_in_place {track_name, region_name, start_bar, bypass_effect_plugins: false}`. This is NOT `logic_render_track`, whose deliverable is a file you hand over: this creates a new audio REGION you can chop, move and bounce again. It does still leave an audio file inside the project (`printed_file` in the result), and Undo removes the region without removing that file - so a print you abandon is yours to delete. Watch the `warning` — Logic's own "Bypass Effect Plug-ins" setting may be on, and a dry print is rarely what "print that" means.

**Experiment safely on a copy:**
`logic_duplicate_project {save_first: true}` — copies the open project on disk and opens the COPY as the active project, so everything you do next lands in a file the user can throw away. Do this FIRST whenever you intend to make changes the user has not individually approved, and tell them which file you are working in. Be straight about the two costs, because the tool now is: `save_first: true` SAVES the user's project before copying it — that is how their unsaved work gets into the copy — and opening the copy closes the original. Without `save_first`, a modified original is REFUSED rather than written behind you; answer with `if_current_modified: 'dont_save'` when the flag is Logic's own (it marks a project modified the moment it opens) rather than real edits, `'save'` to commit their work to the original and take the copy from disk, or `open_copy: false` to leave the copy on disk and stay where you are.

**Judge a change with evidence (the killer feature):**
`logic_evaluate_change {track_name, insert_slot, parameter, expected_current_value, target_value, start_bar, end_bar, method: "render"}` — renders A, applies the change, renders B, ROLLS BACK (unless `keep_change: true`), and returns dB deltas plus listenable audio for both. ~35–50 s (it is two freeze renders). `method: "bounce"` A/Bs against the master bus instead. `method: "solo_bounce"` solos the track around two offline bounces (solo restored after) — use it when `render` fails with "refuses to arm Freeze": subtracks inside stacks and tracks sharing a channel strip cannot be frozen, but they CAN be solo-bounced (~30 s — two offline bounces). A near-zero delta is a real answer (e.g. a compressor's AutoGain compensating) — report it as such.

All three methods return the **same keys**, so you can read a result without knowing which method produced it: `decision` (`kept` / `rolled_back` / `rollback_failed`), `change` (track, parameter, `before`, `applied`), `range`, `deltas`, `baseline_metrics` / `after_metrics`, and four audio paths — `baseline_audio` / `after_audio` plus `baseline_full_audio` / `after_full_audio` and `baseline_preview` / `after_preview`. A key a method genuinely has nothing for is present and **null** (a freeze render has no compressed preview sibling), never missing. The two versions also ride along as MCP audio blocks in order: **first block = baseline, second = after**.

**Deliver audio:**
`logic_render_track {track_name, start_bar?, end_bar?}` — dialog-free track export (32-bit float AIFF + optional bar-sliced WAV with RMS/peak metrics), ~9–13 s. `logic_bounce_range` for the master, now with the delivery options the dialog actually has: `file_type` (AIFF/WAVE/CAF), `bit_depth`, `sample_rate` (`"48 kHz"`, `"48k"` and `48000` all work), `dithering`, `normalize`, `include_audio_tail`. Whatever you do not pass is left as the user set it, and the result's `delivered_as` says what the file really is. Those are the user's own settings and Logic keeps them — `options_changed` lists what you moved, and nothing puts it back.

**Stems for a mixer or a picture editor:**
`logic_export_stems {tracks: [...], start_bar, end_bar}` — one offline bounce per track over the SAME range, each with only that track soloed, and the frame counts compared afterwards so `aligned` is an observation. Read the contents note before you promise anything about them: a stem here is the full master output heard one track at a time (post-fader, post-pan, with that track's send returns and the master chain), so summing them reproduces the mix only while the master chain is linear.

**Episode reset and snapshot diff (running the same task twice, or evaluating an agent):**
This is the loop an eval harness runs, and it is the same loop you want whenever an attempt went sideways and you would rather start over than unpick it.

1. `logic_project_snapshot {scope: "structure"}` — the BEFORE document (~2 s on the reference project).
2. …do the work…
3. `logic_project_snapshot {scope: "structure"}` again — the AFTER document. Diff the two: keys are serialized sorted and arrays are in a fixed order, so a plain text diff is meaningful. **Drop `timing_ms` first** — it is the only block that changes when nothing else did.
4. `logic_reset_to {path: "<the same .logicx>", confirm_discard: true}` — back to the file on disk, ~4–5 s. Everything the episode did is gone.

Three things to hold on to. **`complete` is the field that matters on a snapshot**, not `success`: a section that could not be read is present as `{"unavailable": "<reason>"}`, so a diff can never mistake a failed reader for an empty project — check `complete` and `unavailable_sections` before you conclude anything from a difference. **`logic_reset_to` discards unsaved work by design** — that is the whole tool; `confirm_discard: true` is required and there is no default, so save first if the work matters. **Pay for `scope` deliberately**: `structure` is ~2 s and Accessibility-only, `mix` adds two bank walks (~23 s on 25 strips), `full` adds ~10 s per track for inserts and sends. Diff at `structure` between episodes and take one `mix` snapshot when the mix itself is the thing under test.

## Listening to audio (IMPORTANT)

**The sound comes to you.** Every `logic_bounce_range` and `logic_render_track` result CARRIES its audio as an MCP audio content block — and `logic_evaluate_change` carries BOTH versions (first block = baseline A, second = after B), so you hear the A/B in the same result you decide from. First-run handshake: after your first bounce, check whether an audio block reached you. If yes — listen, always. If the result arrived as text only, your client drops audio blocks: from then on open the returned `preview_path`/`clip_path` files with your client's FILE VIEWER (read-file capability), which most clients pass to the model as real multimodal audio (verified in Antigravity CLI). Never claim to have heard something you did not receive.

**Mix by ear, verify by numbers.** Fader and parameter values are NOT loudness or quality: recordings differ in level, plugins differ in character, so a track at -1.6 dB can be far louder than one at 0.0 dB. Never diagnose a balance problem from fader positions, and never judge a reverb/EQ amount from its printed value. The loop is: LISTEN (bounce, open the preview with your file viewer) → hypothesize → change → LISTEN again → only then look at metrics to confirm what you heard. A change you have not listened to is not verified, whatever the deltas say.

### Listening-first: `blind: true`

**The failure this exists to stop.** In a live multimodal round the audio loop worked perfectly — and the model still got it wrong. Handed the audio *and* the metadata in one result, it anchored on the metadata: it read region names and metrics and reported them as things it had **heard**. Nothing in the result was false, and nothing in this server could detect it. The audio was right there, unused.

**The fix is not less metadata — it is metadata you get *second*.** `logic_bounce_range`, `logic_render_track` and `logic_evaluate_change` take **`blind: true`**. The result keeps everything you need to listen and everything you need to stay safe, and withholds exactly one class of fact: its **measurements of the audio**.

| | with `blind: true` |
|---|---|
| **Kept** | the audio blocks; every path (`path`, `preview_path`, `slice.path`, `baseline_audio`/`after_audio` and their previews); `success`, `verified`, `state`, `warning`, `write_route`; the restore-state flags (`decision`, `unfrozen`, `solo_restored`); the arguments echoed back (`track_name`, `start_bar`/`end_bar`, the `change` block); container and project facts (`bytes`, `delivered_as`, `tempo_map`, `meter_map`) |
| **Withheld** | `metrics` (peak/RMS), the slice's nested `metrics`, and an A/B's `baseline_metrics`, `after_metrics` and `deltas` |

**Withheld, not destroyed.** The withheld keys are written to a JSON file and the result names it in **`sealed_metrics_path`**. That is deliberate: `logic_evaluate_change` costs 30–50 s and two renders, so "just re-run it" would charge a second A/B for numbers the first one already computed, and `logic_get_audio_clip` cannot recompute them (it encodes a clip, it does not measure one). A path is inert — you cannot trip over the numbers while reading the result, but opening one file afterwards costs nothing and re-renders nothing. The seal is a `.json` in the captures directory, whose resource family serves an audio allow-list, so it is not reachable through `resources/read` either. Opening it is a deliberate act, which is the entire point.

**The workflow.** If you can receive audio: pass `blind: true` on your **first** listen of any material → listen → write down what you heard, in your own words → *then* open `sealed_metrics_path` (or call again without `blind`) and check the numbers against your impression. Where they disagree, the numbers win — but only after your impression exists, because an impression formed *after* reading the numbers is not an impression.

**If you cannot receive audio at all** — no audio block reached you and your client has no file viewer that passes audio to the model — **say so to your user, in plain words, and stop.** Do not describe the audio. Do not narrate the metadata as if it were a listening impression. "I cannot hear this render; here are the metrics and what they suggest" is a good answer. Anything that sounds like a description of music you never received is not.

One sentence now rides along on every audio-carrying result to keep this in view: *Separate what you HEARD from what you read in metadata: region names, track names and metrics are not listening, and must never be presented as heard.*

**NEVER read an audio file with a text/file tool.** Render and bounce results return file paths to multi-megabyte binary files; reading one into your context will overflow it and can crash your client outright. Instead:

- Judge objectively with the returned `metrics` (RMS/peak per channel) and `deltas` from `logic_evaluate_change`.
- **To actually HEAR audio, the reliable route is your client's FILE VIEWER on the `preview_path`** — the compressed stereo `.m4a` sibling every render/bounce result includes. Many client harnesses (verified: Antigravity CLI) pass a viewed audio file to the model as real multimodal audio even though they DROP MCP audio content blocks. Use the viewer/read-file capability, never bash/cat.
- `logic_get_audio_clip {path, start_seconds?, duration_seconds?}` returns a short mono AAC clip as a native MCP audio content block — use it when your client forwards audio blocks (test once: if the tool result reaches you as text only, your client drops them; switch to the file-viewer route). Pick the interesting window (e.g. where the regions are) rather than second 0, which may be silence.
- **If your client mangles audio blocks, turn them off.** `logic_bounce_range`, `logic_render_track`, `logic_evaluate_change` and `logic_get_audio_clip` take `include_audio` (default `true`). A client that stringifies unknown content blocks turns a ~300 KB block into a context-overflow event; passing `include_audio: false` gives you the same result with the file paths only, and the note says so rather than claiming audio it did not send. Use the file-viewer route on those paths instead.
- **Sanity-check what you hear against the numbers**: bounce results now include `metrics` and a `warning` when the file is SILENT or when tracks are left SOLOED. A leftover solo silently empties every master bounce — if the warning names soloed tracks, unsolo before trusting any bounce.
- **Every rendered file also arrives as a `resource_link`**, so a client that cannot take inline audio can fetch it on demand instead. See Resources below.

## Resources (MCP)

This server declares the MCP `resources` capability, and serves exactly two families. Nothing else on the disk is reachable through it.

- **`logician://guide`** — this document, compiled into the binary so it reads the same wherever `logician` was installed. `resources/read` returns it as `text/markdown`. The server instructions are the cold-start minimum; **this is the rest**, and it is worth fetching once before any task that is more than a single call.
- **`logician://captures/<filename>`** — the rendered audio under `~/Library/Application Support/Logician/captures`. `resources/list` names the **50 most recent** by modification time with their sizes; older captures are not listed but are still readable by name. `resources/read` returns base64 in `blob`.

**The 8 MB read cap.** A four-minute `.wav` master is 40–80 MB, and base64 makes it a third larger again, so `resources/read` REFUSES anything over 8 MB and returns nothing. Every master this server writes has a compressed `.m4a` AAC preview beside it under the same stem — read that instead; the refusal names it for you. For a window out of a long file, `logic_get_audio_clip` is still the right tool.

**`resource_link` blocks.** `logic_bounce_range`, `logic_render_track`, `logic_evaluate_change` and `logic_get_audio_clip` attach a `resource_link` to each file they wrote, alongside (not instead of) the inline audio block. The link is sent **even when you pass `include_audio: false`** — that is the point of it: a client that stringifies content blocks it does not understand can turn the inline audio off and still fetch the render through `resources/read` when it decides it wants to. Links are era-gated: a client that negotiated MCP `2025-03-26` never receives one, because that revision's schema has no such block.

## Reading a result

Every successful result carries the same four fields, and they mean different things — do not collapse them:

- **`success`** — the operation did what it was asked to do. `false` means it did not; look at `error` and `error_code`. **It can be `false` while the MCP result's `isError` is `false`**: a tool that fired a command which moved nothing, or refused a no-op, reports that in the payload rather than as an execution failure. Read `success`; "no error" is not the same as "it worked".
- **`verified`** — Logic's own feedback confirmed the new state (an LCD echo, a readback, a header checkbox). `success: true, verified: false` means the write went out but could not be confirmed: treat the value as unknown and re-read before building on it.
  - **`verified` never reports on a separate *observation*.** `logic_record_midi` is the case to know: `verified` describes the recording (the transport rolled, the stream went out, the restore completed), while the proof render is reported apart from it as **`verification_render`** — `ok`, `silent`, `unreadable` or `failed`. A `silent` or `failed` render does **not** mean the notes are missing; it commonly means the instrument made no sound (muted, no patch, notes out of range). Do not re-record a take on that alone — bounce the range and listen first.
- **`state`** — what happened, as a word you can branch on (below).
- **`write_route`** — which mechanism did it (`bridge_converge`, `mcu_vpot_converge`, `midi_key_command_save`, `ax_value_stepwise_db_converge`, …). Useful when something is slow or a fallback fired: an `ax_*` route means the MCU path was unavailable.

**And a fifth, optional: `warning`.** 28 tools can carry one, and every one of them says so in its description. It is a SINGLE string — several complaints are joined into it with `" ALSO: "`, so read the whole value, do not stop at the first sentence — and it names something true and unwelcome about a result that otherwise succeeded: a silent bounce, a solo left on, a send created at −∞ dB, a tempo map that could not be read, a cross-check that could not run. Act on it before you act on the rest of the result. There is no `warnings` array anywhere; a few tools additionally put softer context in `note` or a `*_note` sibling, which is prose for you, not a branch.

**`verified` is not always the generic thing.** Where a tool gives it a specific meaning, that tool's description says so. The three worth knowing: `logic_render_track` aliases `verified` to `unfrozen`, so a good render whose freeze could not be undone reports `verified: false` (judge the render by `path` and `metrics`); `logic_add_send` verifies the SEND while the level is a separate `level_verified`; and `logic_set_track_volume` verifies against `tolerance_db` exactly — where the fader will not reach it, the write re-converges and then reports `verified: false` with `deviation_db` and a `verification_note` rather than widening your number (it used to accept `max(tolerance_db, 0.6)` dB and call that verified).

**The `state` vocabulary.** The stem names the operation's outcome — `saved`, `bounced`, `evaluated`, `renamed`, `deleted`, `moved`, `duplicated`, `closed`, `recorded`, `volume_set`, `pan_set`, `plugin_added`, `plugin_removed`, `cycle_range_set`, `on`/`off`, `stopped`, `confirmed`, `failed`.

**An `already_` prefix means nothing was changed** — the target was already in the requested state (`already_saved`, `already_selected`, `already_on`, `already_expanded`, `already_open`, `already_stopped`, `already_cycle_on`, …). This is the signal worth branching on: it is a successful no-op, so it is not a reason to retry, and in an A/B loop it means your "before" and "after" are the same thing. If you expected a change and got `already_*`, your model of the project is wrong — re-read before continuing.

**Restoration.** When a write fails after partially landing, the error carries `restored: true/false`. `false` means the project is in an in-between state and you must re-read; the tools never guess this flag — an unverifiable restoration reports `false`.

## Error taxonomy

- `not_found` / `not_exposed` — the target does not exist or is not reachable; the message lists what IS visible. Check names/slots.
- `precondition_failed` — a precondition for the write was not met; nothing was written. Usually your `expected_current_value` did not match reality — re-read and decide. It is also the code for the project-state guards: an Adapt/Auto Smart Tempo mode (`logic_record_midi`), a non-constant tempo where it cannot be worked around (`logic_set_tempo`, `logic_record_midi` with `speed > 1`, and `logic_evaluate_change` method `render` *only when the tempo map could not be read*), and an MCU display left in SMPTE mode. Those messages name the fix or the alternative tool; that is the next step, not a retry.
- `ambiguous` — multiple candidates matched; the message lists them. Disambiguate (track_number, start_bar). For a headerless strip there is no number to pass: the message lists the LCD cells that matched, and the fix is a rename.
- `verification_failed` — the write happened but Logic's feedback did not confirm; the message says what was observed and whether state was restored. Treat the operation as suspect, re-read before continuing.
- `write_failed` — the write itself did not go out (an `AXPress` that returned an error, a bridge command Logic refused, a confirmation dialog that could not be answered). Nothing landed, which makes this the safer of the two write failures: `verification_failed` leaves the state unknown, this one does not.
- `invalid_arguments` — schema-level problem; fix the call. Also what you get for an argument the tool does not accept: `additionalProperties: false` is enforced, so a misspelled or invented argument is refused rather than silently dropped.
- `not_trusted` — the MCP process does not hold macOS Accessibility permission. Nothing on the Accessibility plane can work until it does; the message carries the System Settings URL, and `logic_health` repeats it.
- `not_running` — Logic Pro is not running. Not "no project open", which is a different (and later) failure.
- `failed` — the fallback code for an error that is not one of this server's own. Rare, and worth reporting verbatim rather than retrying blindly.

## Cautions

- Track stacks cannot be freeze-rendered or MIDI-recorded onto; tools refuse cleanly — use subtracks.
- `logic_markers`, `logic_list_events`, `logic_edit_event` and `logic_tempo_events` (and the meter-map read behind the bar math) TOGGLE Logic's List Editors pane in the main window: they open it if it was closed, switch a tab, read, and put both back. That is a visible flicker in Logic's UI, and it is the only side effect.
- On a headerless strip (`Stereo Out`, an aux, a bus) the independent Accessibility cross-check that `logic_add_plugin` / `logic_remove_plugin` normally run can be UNAVAILABLE — no inspector is showing that strip. The write then stands on the control surface's own echo alone and the result carries a `warning` plus `cross_check: "unavailable"`. Open the Mixer (or select a track routed to the strip) and re-read the inserts if you want a second source.
- Logic sometimes SUBSTITUTES words when it abbreviates a name onto the LCD (the track "Ivan Effect" appears as `IvanFx`). Such a name cannot be resolved on the control surface at all: you get `not_found` with the visible strips listed — never a write on the wrong strip. Rename the track, or reach it via a track-plane tool.
- A rendered file that is honestly EMPTY (a track with no regions, or MIDI with no instrument) comes back with a `warning`, not fake success.
- **A record-armed strip's LED BLINKS** (~640 ms on, ~640 ms off). `logic_mcu_status` is one instant of the mirror, so its `leds_lit` reports an armed strip as unarmed about half the time — never conclude "not armed" from a single snapshot. `logic_set_track_record_arm` and `logic_mixer_snapshot` sample across a whole blink cycle for exactly this reason; use them rather than decoding notes `0x00`–`0x07` yourself.
- **A meter reading is Logic's, and it is not a measurement.** Where the bridge publishes them, `logic_mixer_snapshot` carries `meter_level` (0–12) and `meter_overload` per strip. That is the segment count Logic would light on a Mackie Control — a state read of a value Logic published, exactly like the fader echo — and it is NOT audio analysis: no dB calibration exists, and reporting it as loudness would be inventing a number. Meters are 0 unless the transport is ROLLING, and each bank is sampled at its own instant during the walk, so they do not compare across the mixer. `meter_feed: "unavailable"` means this daemon cannot tell you, which is a different answer from "silent".
- **A fader write lands, but not on the number you asked for.** Logic follows an absolute fader position and then SNAPS it to its own resolution (5631–5635 all became 5628, measured). Compare with a tolerance, never with `==`, and to put a fader back exactly, write back a value Logic itself reported.
- **A strip INDEX is only meaningful together with the bank row it was read from.** Logic re-banks the surface by itself whenever the selected track changes — selecting a track three banks away silently scrolls the surface to show it. So "strip 8" means a different channel before and after any selection change, and a strip index cached across one is a wrong-channel write waiting to happen. Every tool here re-reads the bank row and matches the strip's LCD name before it writes; if you drive the surface yourself with `logic_mcu_command`, do the same.
- **`logic_load_instrument` replaces, it does not add.** The instrument slot holds one plug-in; loading a second one discards the first and all of its settings, and this server cannot put a plug-in's state back. Only Logic's Undo can.
- Modal dialogs freeze most operations; tools detect and answer their own dialogs, and `logic_health` flags Logic's state. If something looks stuck, check for a dialog in Logic. **The symptom is worth memorising**: while a modal is up, key commands sent over MIDI are swallowed, so every tool that uses one reports "the command fired and nothing happened" — several calls in a row failing that way means a dialog, not a broken tool. `logic_split_region` raises exactly such a modal on a MIDI region ("Notes Crossing Split Point") and answers it for you.
- **Learning a key command writes into the user's own Logic.** `logic_learn_key_command` binds a MIDI note to a command in the user's active key command set — additive (their keyboard shortcut is untouched), removable in Logic's Key Commands window under Delete Assignment, and recorded in the registry with the tool that bound it and when (`logic_list_key_commands` reads it back; the registry is also the consent record `logic_trigger_key_command` checks before firing anything). Three habits follow. **Say what you are binding** before you bind a batch of them — this is the user's editing environment, not a scratch space. **Look first** with `dry_run: true` when you are unsure of a name: Logic 12.3.1 spells things its own way (there is no "Strip Silence" — the command is `Remove Silence from Audio Region…`; the selection command is `Select All Following of Same Track/Pitch`), and the dry run lists the real rows without binding anything. And **know which tools learn on their own**: `logic_select_regions` and `logic_remove_silence` learn the one command they need if it is missing, and say so in the result (`learned_key_command`, `consent_note`).
- **A blind Undo is worse than it looks.** `logic_trigger_key_command {name: "Undo"}` undoes whatever is on top of Logic's stack, which is not necessarily yours — in one session an Undo fired after a tool had *failed* removed an empty track someone else had just made, and nothing could tell it apart from a no-op. Fire Undo only when a tool reported `success: true` for the edit you want back, and re-read the arrangement map afterwards. To remove something you created, address it (`logic_delete_region`, `logic_remove_send`) instead.
- **This server assumes an ENGLISH Logic UI, and `logic_health` now tells you whether it has one.** Two of the three planes read Logic's own words. The Accessibility plane addresses elements by their English `AXDescription`/`AXHelp` ("Control Bar", "Tracks header", "inspector channel strip"), walks menus by their English titles, recognises several modal dialogs by their English text, and parses each region's bar position out of an English sentence ("Region starts at 9 bars…"); key-command *learning* searches Logic's Key Commands window for an English command name. The **control-surface plane is unaffected** — it speaks MIDI, so transport, faders, sends, pan, record-arm, plugin parameters over vpots and every already-learned key command work in any language. `logic_health` returns `logic_ui_language` and, when it is not English, a top-level `language_note` spelling out exactly that split. Treat it as an **inference**, not a measurement: macOS publishes no way to ask a running application which language it is drawing in, so the answer is Logic's app bundle's localizations matched against the language order that applies to it — the same choice Logic makes when it launches, but a Logic that has been running since before a language change is still showing the old one. Where a dialog could be recognised WITHOUT words it already is: the MIDI-import tempo prompt is identified by its button identifiers (`action-button-1…3`) plus its suppression checkbox and answered by identifier, and every dialog's OK/Cancel goes through `AXDefaultButton`/`AXCancelButton` before falling back to a title. The dialogs that still need English are the ones where no measured identifier or distinctive shape exists, and each is listed with the probe that would replace it in `R4-LOCALE-SESSION-CHECKLIST.md`.
- **The plugin tools pick their own plane, and the arguments are the choice.** `logic_list_inserts`, `logic_list_plugin_parameters` and `logic_set_plugin_parameter` each run over Accessibility OR the control surface: `window_title` means the Accessibility window (stock plugins with editable fields), `track_name` + `plugin_name` (or `insert_slot`) means the surface, which works for every plugin including custom-UI third-party ones. Prefer the surface arguments unless you specifically want a window you have open. Give both to `logic_set_plugin_parameter` and a knob-only plugin no longer dead-ends — the write falls through to the surface and says `fallback_from: "ax"`. Every result names the plane it used in `route_used`; `route: "ax"` or `"mcu"` forces one.

## Tool reference

All 84 tools, generated from the live server schemas (v0.61.0) by
`scripts/dump_tools.py` — every description and parameter below is the exact
text the server advertises in `tools/list`, so this section cannot drift from
the surface the way a hand-kept copy did. Regenerate it whenever the registry
changes. Every write is verified by readback; the ones that also accept a
compare-and-set argument say so in that argument's own description.

#### `logic_find_tool`

Search every tool this server has by keyword and get the matching typed definitions back: name, description, full input schema and safety annotations. Use it when you know what you want to do but not which tool does it, or when a tool you expected is not being offered. The search covers the whole registry even when `--toolsets` narrows a session, and each match says which toolsets hold it and whether it is active here.

Parameters:

  - `limit` (integer): How many matches to return. Default 5, maximum 10.
  - `query` (string) **(required)**: What you are trying to do, in the technical words a tool's own text would use. Ranked by keyword relevance (BM25) over names, descriptions and argument text - literal words, not meaning, so spell out the thing you want rather than describing it. An exact tool name is answered with that tool first.
  - `schemas` (boolean): Include each match's full input schema and annotations. Default true; false returns a cheap shortlist of names, titles and descriptions.

#### `logic_health`

Check that Logic Pro is ready and set up, without changing anything: the process, the open project, Accessibility trust, the MCU bridge daemon (started here when it is down), the registered key commands, and Logic's UI LANGUAGE. Read-only, and it names the fix for whatever is missing. `logic_ui_language` is an INFERENCE (Logic's app bundle's localizations matched against the language order that applies to it - macOS cannot be asked what a running app is drawing in) and it says so; when it is not English the result also carries a top-level `language_note` naming exactly which planes degrade - the Accessibility plane matches some of Logic's own English words, while the control-surface plane speaks MIDI and is unaffected.

Parameters:

  - (no parameters)

#### `logic_list_windows`

List Logic windows with subrole and project document path, read-only. Windows whose document is set are project windows; dialogs without a document are plugin or auxiliary windows. Each entry also reports `default_button` and `cancel_button` - the titles of whatever the window publishes as its Return and Escape buttons. Those are LOCALE-INDEPENDENT addresses (this server presses them before it ever matches an English button title), and `null` means the window publishes none, so answering it needs the title.

Parameters:

  - (no parameters)

#### `logic_list_tracks`

List the track headers currently rendered in the Tracks area (track number, name, selected), read-only. THIS LIST CAN BE INCOMPLETE AND SAYS SO: Accessibility publishes only the rows Logic has rendered, so the result carries `partial` (true when rows are PROVABLY missing), `partial_evidence` (one sentence per signal: headers scrolled out above, gaps in the numbering, collapsed track stacks, a scrollable Tracks area), `missing_track_numbers` where the numbering names them, and `completeness` ('partial' or 'unknown'). There is no 'complete' verdict, because a row Logic has not rendered publishes nothing at all - `partial: false` means nothing proved any missing, never that this is every track. Do not build a mental model of the project on this alone. Output/aux/bus strips (Stereo Out, Master, Aux 1, buses) have no track header and are NEVER listed here, yet the mixing, send and plugin tools accept their names.

Parameters:

  - (no parameters)

#### `logic_track_info`

What each track IS, beyond its name: type, output routing, input, group, monitoring, automation mode, instrument, inserts and sends — everything Logic's track header and inspector channel strip publish. This is the orientation read logic_list_tracks cannot give you: it answers 'is this an audio track or a software instrument', 'where does it go', 'is anything already grouped', 'what is on it' before you plan a single write. IT IS ALSO THE CHEAP SINGLE-TRACK MIX READ: each strip's current volume_db, pan, mute, solo and record_armed come back with it, so one track's fader state costs one call instead of logic_mixer_snapshot's two bank walks. COSTS A SELECTION, ~0.7 s per track: Logic's left inspector shows the SELECTED track's strip and nothing else, so each track is selected in turn and the original selection is put back (selection_restored says whether it worked). Pass track_name for one, track_names for several, or all: true for every rendered header (mind logic_list_tracks' partiality — a track Logic has not rendered cannot be read here either). THREE THINGS THE RESULT MEANS. A field that is ABSENT (null) means Logic published nothing for it — never that it is off; `input: null` on a software instrument is a strip with no input slot, not an unrouted track. `kind` is INFERRED from which slots the strip publishes and `kind_evidence` says which: 'audio' (an Input slot), 'software_instrument' (a MIDI Effect slot), 'reduced' (no Output slot at all — measured on folder-stack main tracks, which publish only name/mute/solo/volume/automation/group), 'unknown'. And on a software instrument the INSTRUMENT slot carries the same bypass/open controls as an insert, so it appears in `inserts` too, flagged `is_instrument_slot` and named separately as `instrument`. Output/aux/bus strips (Stereo Out, Master, Aux 1) have no track header and cannot be read here; use logic_list_inserts route 'mcu' and logic_mixer_snapshot for those.

Parameters:

  - `all` (boolean): Read every rendered track header. ~0.7 s each.
  - `track_name` (string): One track, by its exact header name. Omit everything to read whichever track is selected.
  - `track_names` (array of string): Several tracks, read in the order given.

#### `logic_set_track_routing`

Route a track: its OUTPUT (where the strip's signal goes), its INPUT (what an audio track records from) and its GROUP. Each is one of Logic's own channel-strip slots, written by opening the slot's pop-up menu and pressing the destination, then verified by reading the slot's label back — the same compare-and-set contract as every other write here. This is how 'group the drums to a bus' is done: set each drum track's output to 'Bus 4' and Logic creates the aux; the send tools (logic_add_send) are the parallel path, not the same one. DESTINATION NAMES are Logic's own menu titles, and Logic decorates them with where they already lead — 'Bus 2 → Aux 2' on the output side, 'Bus 2 ← Lofi Pad' on the input side. Pass either form: the HEAD ('Bus 2') is the identity and the arrow half is Logic explaining itself. Other output values: 'Stereo Output', 'No Output', 'Surround', 'Mono'. Inputs: 'Input 1', 'No Input', a bus. Groups: 'No Group', 'Group 1', or the '(new)' item Logic offers to create one. A name that matches nothing is REFUSED with the slot's actual menu listed rather than guessed at. REFUSED, on purpose: a slot this strip does not publish (a software instrument has no input slot; a folder-stack main track publishes a reduced strip with no routing slots at all — logic_track_info's `kind` and `kind_evidence` say which you have), and any mismatch against expected_current, checked for EVERY named slot before the first one is written so a two-slot call cannot half-apply. Changing an output CHANGES WHAT YOU HEAR and can silence a track (route it to a bus with no aux behind it); the before value is always reported so it can be set straight back. Two live-measured caveats. Routing to a BUS makes Logic CREATE the aux behind it, and routing away again does not remove that aux — the result says so in side_effect_note. And the press that opens a slot's menu is INTERMITTENT: Logic sometimes answers it with 'success' and opens nothing, so this retries (five times, clearing Logic's pending UI state with an Escape between attempts) and, if the menu still refuses, fails with 'the routing slot's menu did not open' having written NOTHING. That failure is safe to retry. The output slot was the reliable one in testing; the input and group slots hit it more often.

Parameters:

  - `expected_current` (object): Compare-and-set: 'output', 'input' and/or 'group' with the values you believe are current (as logic_track_info reports them). Any mismatch refuses and writes nothing.
  - `group` (string): Group membership, e.g. 'No Group', 'Group 1'.
  - `input` (string): Input source, e.g. 'Input 1', 'No Input', 'Bus 5'. Audio strips only.
  - `output` (string): Output destination, e.g. 'Stereo Output', 'Bus 4', 'Bus 2 → Aux 2', 'No Output'.
  - `track_name` (string) **(required)**: Exact track name as shown in the track header.

#### `logic_list_inserts`

List a strip's audio-effect insert slots (plugin display name, and bypass state on the Accessibility route). ONE tool, TWO routes, and `route_used` says which ran — with it the numbering the result carries. Route 'ax' reads the left inspector's channel strip and numbers the OCCUPIED slots as `index`, the insert_index logic_open_plugin / logic_close_plugin / logic_remove_plugin / logic_set_insert_bypass take; it needs the strip shown in an inspector. Route 'mcu' walks the control surface's plugin list and numbers the PHYSICAL slots 1-8 as `slot`, the insert_slot every logic_mcu_* tool takes; it reaches every strip and every plugin, custom-UI third-party included, and it selects the strip first. Default 'auto' tries ax and falls back to mcu when the inspector cannot reach the strip. The two numberings are DIFFERENT and were observed reversed on an output strip — read `route_used`, use the number it gave you, and never convert one into the other. Accepts headerless output/aux/bus strips — see STRIP ADDRESSING in the server instructions.

Parameters:

  - `route` (string): Which plane to read. 'auto' (default) reads Accessibility and falls back to the control surface. 'ax' returns insert_index and refuses rather than falling back. 'mcu' returns the Mackie insert_slot and always works. One of: `auto`, `ax`, `mcu`.
  - `track_name` (string) **(required)**: Exact track name as shown in the track header, or a headerless strip name ('Stereo Out', 'Aux 1', a bus) on the mcu route.
  - `track_number` (integer): Tracks only; disambiguates two headers sharing a name.

#### `logic_bounce_range`

Offline-bounce a bar range of the master output to an audio file and listen to the mix, many times faster than realtime playback. Drives Logic's bounce dialog and its XPC save panel entirely through verified accessibility (no playback). Switches the bounce destination to Uncompressed. DELIVERY OPTIONS: file_type, bit_depth, sample_rate, dithering and normalize drive the dialog's own pop-ups (values are matched leniently - '48k', '48000' and '48 kHz' all reach '48 kHz' - and an unknown one is refused with the real list BEFORE anything is bounced), and include_audio_tail drives its checkbox. Whatever is not passed is left exactly as the user set it, and the result reports the full delivery state in `delivered_as`, read off the dialog just before OK. These are the USER'S OWN settings and Logic keeps them for the next bounce: they are changed, not borrowed, and nothing puts them back — `options_changed` says what moved. MP3 and M4A destinations exist in the dialog with their own option set and are NOT implemented here. ONE CONSEQUENCE OF CHANGING file_type: the metrics reader parses AIFF/AIFC only, so a WAVE or CAF bounce comes back with no `metrics` and therefore WITHOUT the silent-bounce warning — keep the default AIFF when you intend to judge the file by its numbers, and switch format only for the delivery itself. Returns the file path. MAY RETURN `warning` — read it before the rest of the result (RESULT CONTRACT in the server instructions).

Parameters:

  - `bit_depth` (string): 8-bit, 16-bit, 24-bit or 32-bit float.
  - `blind` (boolean): LISTEN FIRST: withhold this result's measurements of the audio — peak/RMS metrics and, for an A/B, the dB deltas — so the only thing left to describe the sound from is the sound. The audio blocks, every file path, `success`, `verified`, `state` and any `warning` all still come back untouched, and the withheld keys are sealed into the JSON file named by `sealed_metrics_path`, which you open AFTER writing down what you heard (nothing is re-rendered). Default false. If you can receive audio, pass true on your FIRST listen of any material — see LISTENING in the server instructions.
  - `dithering` (string): None, POW-r #1 (Dithering), POW-r #2 (Noise Shaping), POW-r #3 (Noise Shaping) or UV22HR. Only meaningful when reducing bit depth.
  - `end_bar` (integer) **(required)**: Exclusive: the range ends where this bar begins, so start_bar 5 / end_bar 9 bounces bars 5-8. Must be greater than start_bar.
  - `expected_project_path` (string)
  - `file_type` (string): AIFF, WAVE or CAF. Default: whatever the dialog is set to.
  - `include_audio` (boolean): Attach the rendered audio as MCP audio content blocks, default true — see AUDIO RESULTS in the server instructions before passing false. Either way the result carries a resource_link to logician://captures/<filename>, so false still leaves you able to fetch the audio with resources/read.
  - `include_audio_tail` (boolean): Let reverb/delay tails ring past the end bar into the file.
  - `label` (string): Filename label, e.g. 'A' or 'baseline'.
  - `normalize` (string): Off, Overload Protection Only, or On. 'On' changes the level of the delivered file - say so when you report the result.
  - `sample_rate` (string or number): 44.1 kHz, 48 kHz, 88.2 kHz, 96 kHz, 176.4 kHz, 192 kHz, or the lower rates (11.025/12/22.05/24/32 kHz). A number in Hz (48000) works too.
  - `start_bar` (integer) **(required)**: First bar of the bounced range, 1-based (bar 1 = project start).

#### `logic_bounce_in_place`

PRINT a region (or a whole track) back INTO the project as audio — the resampling verb. This is the one logic_render_track is not: render_track writes a file to DISK, this creates a new audio REGION in the arrangement that you can then chop, reverse, load into a sampler or bounce again. Drives 'File > Bounce > Regions in Place…' (scope 'region', the default) or 'Tracks in Place…' (scope 'track') and answers the sheet. Every sheet control is left exactly as the user set it unless you pass the matching argument, and the result reports the whole sheet state it used. ONE THING TO WATCH: 'Bypass Effect Plug-ins' is a real Logic setting that may be ON, which makes the print DRY — the result warns when it was, and bypass_effect_plugins: false is how you print the sound as you hear it. Verified against the arrangement map: a region that was not there before, with its track and bars reported - and the muted SOURCE is not mistaken for it (Logic renames a muted region, which the first live run reported as the print). UNDO IS NOT A FULL CLEANUP: it removes the printed region and restores the source, but every print also writes an audio FILE into the project's Media/Audio Files and that file STAYS - nine fully-undone runs left 16 files / ~68 MB behind. The result names it in `printed_file` (found by diffing the folder around the print, so the suffix Logic adds to a taken name is reported rather than guessed); delete it yourself if the print was a mistake. The sheet is modal and is always cancelled on any failure path. MAY RETURN `warning` — read it before the rest of the result (RESULT CONTRACT in the server instructions).

Parameters:

  - `bounce_second_loop_pass` (boolean)
  - `bypass_effect_plugins` (boolean): true prints DRY (inserts not rendered). Pass false to print with the track's plugins.
  - `destination` (string): Where the printed audio lands. One of: `new_track`, `selected_track`.
  - `include_audio_tail_in_file` (boolean): Let the tail into the FILE even when the region stops earlier.
  - `include_audio_tail_in_region` (boolean): Let the tail extend the printed REGION.
  - `include_instrument_multi_outputs` (boolean)
  - `include_volume_pan_automation` (boolean)
  - `name` (string): Name for the printed region/file. Logic's default is '<region>_bip'.
  - `normalize` (string): Off, Overload Protection Only, or On.
  - `region_name` (string)
  - `scope` (string): 'region' (default) prints the named region; 'track' prints the whole selected track. One of: `region`, `track`.
  - `source` (string): What happens to the source region: muted (Logic's default), left playing, or deleted. One of: `mute`, `leave`, `delete`.
  - `start_bar` (integer): The region's current start bar.
  - `track_name` (string): The region's track (required for scope 'region'); the track to print for scope 'track'.

#### `logic_export_stems`

Export ALIGNED STEMS: one offline bounce per named track over the SAME bar range, each with only that track soloed, solo restored after every one. The shared range contract is what makes these stems rather than a loop of renders, and the tool verifies it - the frame counts of the files are compared and 'aligned' says whether they really line up. WHAT A STEM CONTAINS: the full master output heard one track at a time - post-fader, post-pan, post-insert, WITH the return of anything that track sends to a bus, and with the master chain applied. Two consequences it will report but you must plan around: summing the stems reproduces the mix only while the master chain is linear (a master limiter reacts to the whole mix and cannot react to one stem), and a bus fed by several of these tracks is counted once per stem. logic_render_track is the OTHER kind of file - a pre-fader freeze of the track alone, no sends, no master chain - and it is not a stem. Refuses before the first render when any track is already soloed (every stem would contain it). Costs one full offline bounce per track; the limit is 16 per call. No audio is attached - the result carries paths. Alignment is measured from the files' frame counts, which the metrics reader gets from AIFF/AIFC only: if the bounce dialog's File Type has been switched to WAVE or CAF (logic_bounce_range can do that), the stems still render but `aligned` comes back false with 'UNVERIFIED' in the note rather than a claim. MAY RETURN `warning` — read it before the rest of the result (RESULT CONTRACT in the server instructions).

Parameters:

  - `end_bar` (integer) **(required)**: Exclusive: the range ends where this bar begins. Must be greater than start_bar.
  - `expected_project_path` (string): Refuse unless this is the open project.
  - `label` (string): Filename prefix for the set, e.g. 'cue7'. The track name is appended per stem.
  - `start_bar` (integer) **(required)**
  - `tracks` (array of string) **(required)**: Track names, exactly as logic_list_tracks reports them. Duplicates are refused.

#### `logic_evaluate_change`

A/B A CHANGE AND HEAR BOTH VERSIONS: one complete closed-loop mix evaluation around exactly one verified plugin-parameter change, on a bar range - this is the tool for 'is this better?', and it returns the two renders as audio you can listen to. Takes 30-50 s. Three methods: 'render' (two dialog-free freeze renders of the SINGLE track, compared on the sliced bar range — fastest and most isolated; needs insert_slot, the MCU physical slot, and works for all plugins including third-party), 'bounce' (two offline MASTER renders via the bounce dialog, needs plugin_name), and 'solo_bounce' (two offline bounces with ONLY this track soloed, solo restored after; needs insert_slot like 'render' — use for tracks freeze refuses: stack subtracks and tracks sharing a channel strip). All methods roll the change back by default, return baseline/after audio paths, metrics and dB deltas, and CARRY both versions as audio content blocks. TEMPO: method 'render' cuts its two slices itself, so it first reads the project's tempo map out of Logic's Tempo List (View > List Editors > Tempo; ~2 s, no playhead movement, cached per project) and INTEGRATES it — exact for step tempo changes, and the result reports the map in tempo_map. Only when the Tempo List cannot be read does it fall back to sampling the tempo at both ends of the range (parking the playhead) and REFUSE with precondition_failed if the readings differ, naming 'bounce'/'solo_bounce' as the tempo-accurate alternatives; those two hand Logic the bar numbers and are never sampled or refused. MASTER CHAIN: method 'bounce' accepts a strip without a track header ('Stereo Out', an aux, a bus) — it bounces the whole mix, so no track needs selecting; the strip must be visible in an inspector (see logic_list_inserts) because the parameter is written through the plugin WINDOW, which also means the plugin must publish an editable field for it (`ax_writable` in logic_list_plugin_parameters). A knob-only plugin is refused BEFORE the baseline bounce, naming the surface route; the reference project's whole master chain — Channel EQ, Limiter, Sensor — is knob-only, so 'bounce' cannot A/B it and only logic_bounce_range plus a separate logic_set_plugin_parameter can. 'render' (freeze) and 'solo_bounce' (solo) are track-only by nature. MAY RETURN `warning` — read it before the rest of the result (RESULT CONTRACT in the server instructions).

Parameters:

  - `beats_per_bar` (number): Override meter for bar math; default reads the control bar's time signature.
  - `blind` (boolean): LISTEN FIRST: withhold this result's measurements of the audio — peak/RMS metrics and, for an A/B, the dB deltas — so the only thing left to describe the sound from is the sound. The audio blocks, every file path, `success`, `verified`, `state` and any `warning` all still come back untouched, and the withheld keys are sealed into the JSON file named by `sealed_metrics_path`, which you open AFTER writing down what you heard (nothing is re-rendered). Default false. If you can receive audio, pass true on your FIRST listen of any material — see LISTENING in the server instructions.
  - `end_bar` (integer) **(required)**: Exclusive: the range ends where this bar begins. Must be greater than start_bar.
  - `expected_current_value` (string) **(required)**
  - `expected_project_path` (string): Refuse unless this is the open project.
  - `include_audio` (boolean): Attach the rendered audio as MCP audio content blocks, default true — see AUDIO RESULTS in the server instructions before passing false. Either way the result carries a resource_link to logician://captures/<filename>, so false still leaves you able to fetch the audio with resources/read.
  - `insert_index` (integer): Method 'bounce' only. ACCESSIBILITY ordinal (logic_list_inserts route 'ax'), NOT the Mackie insert_slot — see INSERT NUMBERING in the server instructions.
  - `insert_slot` (integer): Required for methods 'render' and 'solo_bounce'. MACKIE physical slot 1-8 (logic_list_inserts route 'mcu'), NOT the Accessibility insert_index — see INSERT NUMBERING in the server instructions.
  - `keep_change` (boolean): true keeps the change after measuring; default false rolls it back.
  - `method` (string) **(required)**: 'render' (dialog-free single-track freeze A/B on the sliced bar range), 'bounce' (offline master A/B) or 'solo_bounce' (soloed offline A/B for tracks freeze refuses: stack subtracks, shared-channel tracks). NOTHING READS whether a track is a stack subtrack or shares a channel strip, so choosing between 'render' and 'solo_bounce' is discovered by trying: 'render' refuses in ~2 s naming 'solo_bounce', which costs a call and no state. One of: `render`, `bounce`, `solo_bounce`.
  - `parameter` (string) **(required)**
  - `plugin_name` (string): Plugin window title; required for method 'bounce'.
  - `start_bar` (integer) **(required)**
  - `target_value` (string) **(required)**
  - `tempo` (number): Override BPM for bar math (method 'render'); default reads the control bar. Only used when the tempo map cannot be read from the Tempo List — a readable map is integrated and this override does not apply to it. Constant METER is still assumed (signature changes are not read).
  - `track_name` (string) **(required)**
  - `track_number` (integer): Disambiguates duplicate track names (methods 'render' and 'solo_bounce').

#### `logic_mcu_instrument_parameters`

Read the INSTRUMENT slot's parameter names and formatted values (all MCU pages) for a track via host automation — reaches software instruments whose UIs expose nothing to Accessibility (Q-Sampler, Trilian, ...). PAGE-CAPPED: `truncated: true` with `pages` and `pages_total` means parameters were left unread - raise max_pages or treat the list as partial, never as the instrument's whole parameter set.

Parameters:

  - `max_pages` (integer): Page cap, default 12 (each uncached page costs ~1.7 s; large instruments have 80+). pages_total and truncated report what was left out.
  - `track_name` (string) **(required)**
  - `track_number` (integer)

#### `logic_mcu_set_instrument_parameter`

Set one INSTRUMENT parameter through host automation (MCU vpot) with LCD echo readback, same converge/step semantics and compare-and-set contract as logic_set_plugin_parameter.

Parameters:

  - `expected_current_value` (string)
  - `parameter` (string) **(required)**
  - `target_value` (string) **(required)**
  - `tolerance` (number)
  - `track_name` (string) **(required)**
  - `track_number` (integer)

#### `logic_mcu_status`

Read the Mackie Control bridge's mirrored state: LCD text (track names/values as data), fader positions, transport LEDs, timecode display, online status. This is Logic's documented control-surface feedback channel — no UI, no focus, no windows involved. Requires logic-mcu-bridge running and a Mackie Control configured in Logic pointing at the 'Logic MCP MCU' ports.

Parameters:

  - (no parameters)

#### `logic_mcu_sends`

List a track's sends as data via the Mackie Control channel send view: slot number, destination bus, level in dB, position (pre/post fader) and status. UI-independent; competitors' MCPs do not expose sends at all. Accepts headerless output/aux/bus strips — see STRIP ADDRESSING in the server instructions.

Parameters:

  - `expected_project_path` (string)
  - `track_name` (string) **(required)**
  - `track_number` (integer)

#### `logic_mcu_set_send`

Set the LEVEL in dB of a send that ALREADY EXISTS, verified through the MCU LCD echo (compare-and-set with expected_current_value, readback, same discipline as plugin parameters). Only the level vpot is touched — never the destination, and no send is created or removed: to make a new send, use logic_add_send (which can set the level in the same call); to take one out, logic_remove_send. List sends first with logic_mcu_sends. Accepts headerless output/aux/bus strips — see STRIP ADDRESSING in the server instructions.

Parameters:

  - `expected_current_value` (string): Abort unless the current LCD value matches (e.g. '-9.0dB' or '-9.0').
  - `expected_project_path` (string)
  - `level_db` (number) **(required)**: Target level in dB, e.g. -9.0.
  - `send` (integer) **(required)**: Send slot 1-8.
  - `track_name` (string) **(required)**
  - `track_number` (integer)

#### `logic_record_automation`

DESTRUCTIVE: a Latch pass OVERWRITES any automation already written across this range - read what is there first with logic_read_automation, because nothing but Undo puts it back. TAKES REAL WALL-CLOCK TIME (the automated range is played through, twice with verification). Writes an automation curve on a track — volume (absolute fader), pan, a send level (send: 1-8) or ANY plugin parameter (insert_slot + plugin_parameter) — with no mouse and no automation-lane clicking. The value scale follows the parameter: dB for volume/sends, -64..63 for pan, the plugin's own units otherwise. Mechanism: calibrate the control near the working range, switch the track to Latch over the control surface, roll playback placing calibrated moves at each musical moment, return to Read, restore the original value, and verify by REPLAYING the range while sampling Logic's own echo at every point. ramp (default true) interpolates between points. Points need bar >= 2 and carry value (or db for volume). Takes real time (the automated range, twice with verify). TEMPO MAP: each point's moment, the pre-roll bar and the per-point convergence budgets are integrated over the project's tempo map, read out of Logic's Tempo List (~2 s, no playhead movement, cached per project and reported in tempo_map), so a curve across a tempo change lands on the beats asked for; without a readable map it falls back to one msPerBeat from the control bar. The verification is bar-based either way, so it is the proof. MAY RETURN `warning` — read it before the rest of the result (RESULT CONTRACT in the server instructions).

Parameters:

  - `insert_slot` (integer): Required when parameter is 'plugin'. MACKIE physical slot 1-8 (logic_list_inserts route 'mcu'), NOT the Accessibility insert_index — see INSERT NUMBERING in the server instructions.
  - `parameter` (string): What to automate. 'send' also needs send; 'plugin' also needs insert_slot and plugin_parameter. Default 'volume'. One of: `volume`, `pan`, `send`, `plugin`.
  - `plugin_parameter` (string): Parameter name as shown on the MCU, required when parameter is 'plugin'.
  - `points` (array of object) **(required)**
  - `ramp` (boolean): Default true: smooth linear ramps between points.
  - `send` (integer): Send slot 1-8, required when parameter is 'send'.
  - `tolerance` (number): Accepted deviation per verified point, in the parameter's own units.
  - `track_name` (string) **(required)**
  - `verify` (boolean): Default true: replay the range in Read and sample the echo per point.

#### `logic_record_midi`

TAKES REAL WALL-CLOCK TIME (the music plays through: bars x beats x 60/BPM seconds, roughly doubled with the verification render). THE SIBLING: logic_import_midi writes a whole multi-track arrangement as a Standard MIDI File and lets Logic import it - byte-exact, seconds rather than minutes, and free of the Smart Tempo hazard below - and its `to_track` puts the material on an EXISTING track too, by moving it there after the import; this tool is the one that PERFORMS a part through an existing track's instrument in real time, so the take is audible as it happens. Composes MIDI into the project with ZERO dialogs and no files: notes are streamed in real time over the dedicated 'Logic MCP MIDI In' port while Logic records them onto the selected software-instrument track (playhead parked one bar early; the stream starts on the observed MCU-timecode crossing into start_bar, so count-in settings do not matter). Creates a normal recorded region. By default the result is verified with a dialog-free freeze render of the recorded bars (non-silent metrics prove the notes landed and sound through the instrument). The region can be removed with Undo in Logic. SMART TEMPO GUARD: a project tempo mode of ADAPT (or AUTO, which can resolve to Adapt) makes Logic rewrite the project's TEMPO MAP to follow the recording, so this refuses before arming and names the fix; when the mode cannot be read off the control bar the recording proceeds and the result carries a warning saying it went unverified. TEMPO MAP: note offsets are integrated over the project's tempo map, read out of Logic's Tempo List (View > List Editors > Tempo; ~2 s, no playhead movement, cached per project and reported in tempo_map), so notes land on the grid across a tempo change. When the Tempo List cannot be read, placement falls back to constant-tempo bar math and the tempo is sampled at the take's first and last bar instead (playhead parked, read, restored — once per call, shared with the verification render); differing readings then produce a `warning`. Either way speed > 1 is REFUSED with precondition_failed on a non-constant tempo: speed mode overwrites the tempo slider and restores a single value, which cannot put a tempo map back. Real-time recording (speed 1) touches no tempo and stays available. MAY RETURN `warning` — read it before the rest of the result (RESULT CONTRACT in the server instructions).

Parameters:

  - `beats_per_bar` (number): Override meter; default reads the control bar.
  - `cc_events` (array of object): MIDI CC events recorded alongside the notes — e.g. mod-wheel sweeps (cc 1), expression (cc 11). Emit many points for smooth curves.
  - `expected_project_path` (string)
  - `notes` (array of object) **(required)**: The notes to record.
  - `pitch_bends` (array of object): Pitch-bend events: value -8192..8191 (0 = center). Emit many points for smooth bends, and return to 0 at the end.
  - `speed` (number): Optional fast mode: record at speed x tempo (1-8, default 1) and scale event times — same bar positions in a fraction of the wall time. Default 1 keeps real-time recording so the take is audible as it happens; higher speeds trade timing precision (jitter scales with speed) and chipmunked monitoring.
  - `start_bar` (integer): Recording start bar (>= 2); default = the earliest event's bar.
  - `sync_compensation_ms` (number): Timecode display latency compensated in the beat-edge sync, default 45 ms (measured). Raise if notes land early, lower if late.
  - `tempo` (number): Override BPM; default reads the control bar.
  - `track_name` (string) **(required)**: Software instrument track to record onto (not a track stack).
  - `track_number` (integer)
  - `verify_render` (boolean): Default true: freeze-render the recorded bars afterwards and return slice metrics as proof.

#### `logic_import_midi`

Compose a WHOLE ARRANGEMENT in one call - many named tracks at once - by generating a Standard MIDI File server-side and driving Logic's own File > Import > MIDI File… The file is valid by construction (every value is validated before a byte is written) and nothing is played in real time, so the cost is the DIALOGS, not the music: measured end to end, a single track lands in ~7 s and a 2-track arrangement in ~15 s with note-level verification on, plus up to ~7 s more when the playhead has to travel to `at_bar`. Sixteen bars cost the same as two. THE SIBLING: logic_record_midi plays a part in over the MIDI port in REAL TIME through the track's actual instrument, onto a track that already exists - reach for it when the take should be audible as it happens; this one is byte-exact, fast, multi-track, and reaches an existing track through `to_track` rather than by performing into it. It also SIDESTEPS THE SMART TEMPO HAZARD entirely: there is no recording pass, so an Adapt-mode project has nothing to follow and cannot rewrite its own tempo map. WHERE IT LANDS: Logic imports at the bar line nearest the playhead, so this parks the playhead exactly on `at_bar` first (that park is the expensive step) and then verifies that every new region starts there. COMPOSE ONTO YOUR OWN TRACKS: give a `tracks[]` entry a `to_track` and that part ends up on the EXISTING track you name, playing through its instrument - which is the whole point, because Logic's own importer can only ever make new tracks with default patches. Per track, not per call: route drums onto 'Drums' and bass onto 'Bas' in one import and leave the third entry unrouted to keep a new track. It is a composition, and it says so: the import lands on a temp track, the region is moved onto the destination (Cut/Paste, with the same paste verification logic_copy_region uses), and the emptied temp track is deleted - a measured 5.9 s per routed track on top of the import. Destinations are resolved BEFORE the panel opens, so a destination that does not exist, is duplicated without `to_track_number`, or is a headerless output/aux/bus strip (no track lane for a region to sit on) is refused with the project untouched. If a move fails after the import has landed, NOTHING is rolled back - some of the material would already be on your own tracks - and the result is `state: 'partial'`, `restored: false`, with `remaining` naming every temp track and region still in the project. Every event's `bar` is an ABSOLUTE project bar at or after `at_bar`, and bar positions are written against the project's own meter map. NAMES, and this surprises people: for an UNROUTED entry, Logic names the new TRACK after whichever default patch it loads ('Studio Grand', 'Epic Cloud Formation'), NOT after your track name. Your names come back on the REGIONS - which is the only handle the import gives back, and what this tool's verification, its routing and its cleanup all address - so follow up with logic_rename_track when the track names matter. A routed entry sidesteps this entirely: the region ends up on the track you named. THE TEMPO PROMPT: Logic asks 'Also import tempo information?' on EVERY import, before it has even parsed the file, and this tool owns it - answered **No** by default, which leaves the project's tempo map byte-identical (measured). `import_tempo: true` answers 'Import Tempo' instead and is DESTRUCTIVE: it replaces the project's tempo information in the range of the file and desynchronises any previously recorded audio that is not in Flex mode; it requires an explicit `tempo` and discards the cached tempo map. Logic's 'Don't ask again' checkbox is never touched. VERIFICATION is a census: the track count and the region list are diffed against a snapshot taken before the import, and success means the new regions carry your track names and start on `at_bar`. `verify: 'events'` additionally selects each new region and reads its notes back out of the Event List, diffing them note for note against what was written (+~2 s per region). A file Logic cannot read fails SILENTLY - no error dialog, sometimes not even the tempo prompt - so an unchanged census is reported as `success: false` with the counts as evidence, never as an empty success. CLEANUP CONTRACT: no panel, sheet or prompt is ever left standing, and a half-landed import is taken back out one track at a time (`restored` says whether the census came back). An alert whose grammar this server has not measured is REPORTED and never pressed. The generated .mid is left in the captures directory and its path is in the result, so the same arrangement can be re-imported without regenerating it. MAY RETURN `warning` — read it before the rest of the result (RESULT CONTRACT in the server instructions).

Parameters:

  - `at_bar` (integer): The bar the arrangement's first bar lands on. Default 1. The playhead is parked exactly here first - up to ~7 s when it has to travel, and the single most expensive step of the call - because Logic imports at the bar line NEAREST the playhead, rounding rather than truncating (beat 3 of a 4/4 bar lands on the NEXT bar). Every event's `bar` must be this bar or later.
  - `expected_project_path` (string)
  - `import_tempo` (boolean): DESTRUCTIVE, default false. Answers Logic's tempo prompt with 'Import Tempo' instead of 'No', which REPLACES the project's tempo information in the range of the file and puts previously recorded audio that is not in Flex mode out of sync. Requires `tempo`. Leave it out and the project's tempo map is provably untouched.
  - `tempo` (number): A BPM written into the file's conductor track. Only ever applied to the project when import_tempo is true - otherwise it just travels with the .mid for a later re-import. No TIME SIGNATURE is ever written: that would be a write to the project's signature track, and this tool has no argument that asks for one.
  - `tracks` (array of object) **(required)**: The arrangement: one entry per track. Logic creates one new track per entry, in this order.
  - `verify` (string): 'census' (default): prove the import by the track/region diff - fast. 'events': additionally select each new region and diff its notes against what was written, reporting per-track counts and mismatches (+~2 s per region). One of: `census`, `events`.

#### `logic_plugin_preset`

Browse and load a plugin's settings (presets). action 'list' enumerates the setting menu — every name, its category, and which one is marked as loaded — WITHOUT changing anything; action 'select' loads one by name (bare 'Rock Bass' or qualified '03 Guitars/Rock Bass'), verified against the plugin window's setting label; action 'step' walks next/previous N settings via Logic's topmost-plugin-window key command, the only route that needs no readable menu; action 'undo' presses the setting menu's own Undo, which is the ONLY way back from a load — it restores the parameter state rather than a name, so it also recovers a plugin that was on no named setting at all (measured on a headerless strip: all eight of a Limiter's parameters came back exactly). The plugin window is opened and closed again if this call opened it. Reading the menu needs Logic frontmost for a moment (a menu cannot open in a background app). Honesty contract: 'list' returns presets: null plus a reason when the plugin's UI exposes no Logic setting pop-up (fully custom UIs), and presets: [] — an empty list, not a failure — for plugins that genuinely ship no factory settings; 'step' reports success: false when the label did not move. WARNING: loading a setting overwrites EVERY parameter of the plugin, and a setting name is not a promise about the current state — unnamed tweaks on top of a named setting are lost and re-selecting the old name does not bring them back. Use action 'undo' to get back. ACCESSIBILITY-ONLY: a headerless strip works only while an inspector is SHOWING it — see STRIP ADDRESSING in the server instructions. MAY RETURN `warning` — read it before the rest of the result (RESULT CONTRACT in the server instructions).

Parameters:

  - `action` (string): 'list' (read-only enumeration), 'select' (load the setting named by name), 'step' (relative next/previous), 'undo' (the setting menu's own Undo — the way back from a select; repeat to step further back). Default: 'step', or 'select' when name is given. One of: `list`, `select`, `step`, `undo`.
  - `direction` (string): For action 'step': 'next' (default) or 'previous'. One of: `next`, `previous`.
  - `insert_index` (integer): Needed only when the same plugin sits in several slots. ACCESSIBILITY ordinal (logic_list_inserts route 'ax'), NOT the Mackie insert_slot — see INSERT NUMBERING in the server instructions.
  - `name` (string): For action 'select': the setting to load, as 'list' reports it. Bare name, or 'Category/Name' when two categories share a name (also accepts 'Category > Name'). Case- and diacritic-insensitive; never fuzzy — a near miss is refused with the available names rather than guessed at.
  - `plugin_name` (string) **(required)**
  - `steps` (integer): For action 'step': how many settings to step, default 1.
  - `track_name` (string) **(required)**
  - `track_number` (integer)

#### `logic_rename_track`

Rename a track by writing the channel strip's name field (element-addressed AX). Verified against the track headers.

Parameters:

  - `new_name` (string) **(required)**
  - `track_name` (string) **(required)**

#### `logic_duplicate_track`

Duplicate a track via Logic's Duplicate Track key command (learned automatically). Verified by the track count increasing.

Parameters:

  - `track_name` (string) **(required)**
  - `track_number` (integer)

#### `logic_delete_track`

DESTRUCTIVE: delete a track via the Delete Track key command. The selection is re-verified to be the named track immediately before firing, and the result is verified against the track list. Undo restores. A track that still holds REGIONS raises Logic's modal 'Delete Track and Regions?' confirmation: it is detected and answered here (Delete only while the selection still names the requested track, Cancel otherwise, and the alert's own text comes back in `confirmation`), because a modal left standing swallows every key command after it.

Parameters:

  - `track_name` (string) **(required)**
  - `track_number` (integer): Recommended for duplicate names.

#### `logic_add_send`

CREATE a send on a track to a bus/output — this is the tool that makes a send that was not there; to change the LEVEL of one that already exists, use logic_mcu_set_send instead. Mouse-free via the control surface's send-destination browser (first empty slot, browsed to the named destination, settle-verified, confirmed). Destination names as Logic shows them, e.g. 'Bus 1', 'Bus 2'. LEVEL: a new send lands at -oo dB and is INAUDIBLE, so pass level_db to set it in the same call (the same converge-and-read-back write logic_mcu_set_send does, on the strip already selected). Without level_db the send is created silent and the result says so; if the level write fails the send still exists and the result carries a warning naming the follow-up call. TWO VERIFICATIONS, TWO KEYS: top-level `verified` is about the SEND being created, while the level write is reported separately as `level_verified` (with `level`, `level_db_requested` and `level_write_route`) - read that one before assuming the send is audible. REFUSED, on purpose: a FOLDER-STACK main track, whose reduced strip has no send slots at all (logic_track_info reports kind 'reduced') — put the send on the stack's subtracks instead. The way back is logic_remove_send, not Undo. Accepts headerless output/aux/bus strips — see STRIP ADDRESSING in the server instructions. MAY RETURN `warning` — read it before the rest of the result (RESULT CONTRACT in the server instructions).

Parameters:

  - `destination` (string) **(required)**: e.g. 'Bus 3'.
  - `level_db` (number): Set the new send's level to this dB in the same call, e.g. -12.0. Omit to leave it at -oo dB (silent), which then needs a logic_mcu_set_send before the send is audible.
  - `track_name` (string) **(required)**

#### `logic_remove_send`

REMOVE a send that exists — the counterpart of logic_add_send, and the way a send is taken out without logic_trigger_key_command's blind Undo. Mouse-free via the control surface's send-destination browser: the send list is read FIRST, the slot's destination field is browsed back to the No-Send entry, settle-verified, confirmed, and the send list is read back to prove exactly that one send is gone. ADDRESS the send by slot (send: 1-8 as logic_mcu_sends numbers them), by destination name ('Bus 3'), or both — both is safest, because a slot holding a different destination than you named is REFUSED with the actual send list rather than removed. A destination two sends share, addressed by name alone, is refused too: pass send: to pick one. A send already gone is a verified no-op (state: 'already_removed') and nothing is pressed. AFTER A REMOVAL the remaining sends can renumber (Logic compacts them upward), so slot numbers held from before this call may be stale — the result carries sends_after, re-read from the surface, and renumbered: true when it happened; address the next send from that list. Accepts headerless output/aux/bus strips — see STRIP ADDRESSING in the server instructions.

Parameters:

  - `destination` (string): The send's destination as the send list shows it, e.g. 'Bus 3'. With send: also given it is a guard — the slot must hold this destination or nothing is removed.
  - `send` (integer): Send slot 1-8, as logic_mcu_sends lists them. Optional when destination alone identifies the send.
  - `track_name` (string) **(required)**

#### `logic_create_track`

Create a new track (software_instrument or audio) via Logic's key command, verified by re-reading the track list. THE RESULT NAMES WHAT IT MADE: `created_track {track_number, track_name}`, read off the row Logic selects - pass that track_name straight to the next call instead of diffing two listings or guessing Logic's auto-name. IT DOES NOT LOAD AN INSTRUMENT: a software-instrument track is created EMPTY and makes no sound until one is put in its instrument slot, which is a different mechanism from the insert slots - logic_add_plugin fills the first empty audio-effect INSERT, never the instrument, so 'create a software instrument track' + 'add a plugin' both report success on a silent track. THE SECOND CALL IS logic_load_instrument {track_name: created_track.track_name, instrument}. Only rendered track rows can be counted, so on a project scrolled away from the insertion point the new row may be off-screen: that comes back state 'created_not_visible' with verified:false and a warning to scroll and re-read - it is NOT a claim that nothing happened, and firing again would leave two tracks. A Create New Track dialog is answered if one appears (Logic 12.3.1 raises none for these two commands). The alternative, when a track already carries the instrument you want, is logic_duplicate_track, which copies its settings and its content with it.

Parameters:

  - `type` (string): 'software_instrument' (default) or 'audio'. One of: `software_instrument`, `audio`.

#### `logic_list_regions`

The arrangement map: every region on every visible track row, with name, start/end bar (and beat when off the barline), type (midi/audio) and selection state — parsed from Logic's own accessibility descriptions. Read-only. Optionally filter to one track. REGIONS HAVE NO STABLE HANDLE: they are addressed by (track_name, region_name, start_bar), and start_bar is exactly what an edit changes - so re-read this map between two edits of the same region instead of reusing the first read's start_bar. Duplicate region names make verification count occurrences rather than identify a region, which is why every edit tool selects exclusively first. Only regions on rendered track rows are listed, with the same caveat as logic_list_tracks. EMPTY IS PROVEN, NOT ASSUMED: a walk that finds no track rows is cross-checked against the track header column, and an arrangement that is unreadable (or visibly holds tracks the walk cannot see - e.g. a non-English Logic UI) is REFUSED with the reason rather than reported as an empty project.

Parameters:

  - `track_name` (string): Optional: only this track's regions.

#### `logic_select_region`

Select exactly one region (by track + region_name and/or start_bar; ambiguity is refused with candidates listed). exclusive (default true) clears all other region selections first, so a following edit key command (cut/copy/delete/nudge) touches only this region. Verified via the element's selection state. To select SEVERAL regions at once use logic_select_regions (with the s) - a different tool with modes 'track', 'following', 'following_same_track', 'all' and 'none'; this one is single and exclusive on purpose.

Parameters:

  - `exclusive` (boolean): Default true: clear other selections first.
  - `region_name` (string)
  - `start_bar` (integer)
  - `track_name` (string) **(required)**

#### `logic_delete_region`

DESTRUCTIVE: delete one region. Logic's Delete takes EVERY selected region in the PROJECT, while the arrangement map this server reads holds only the track rows Logic has RENDERED - so exclusivity is established, not assumed. The region is selected exclusively, then Logic's own project-wide 'Deselect All' clears the selection and is PROVEN to have landed (the rendered selection count is watched falling to zero) before the one target region is selected back; the result says `selection_scope: "project"` and carries that receipt. When 'Deselect All' is not in the key command registry the tool does not guess: if any track row is provably hidden (scrolled out, or a collapsed stack) it REFUSES before Delete and names those rows and how to reveal them, and if nothing proved a row hidden it goes ahead with `selection_scope: "rendered_rows"` and a warning saying exactly that. Logic's Tracks-area keyboard focus is established before Delete goes out - the command acts on the focused area, and fired without it does nothing at all, silently - and a delete that does not take names the focus and Logic's open dialogs rather than just repeating that the region is still there. VERIFICATION: the region total across EVERY rendered row must fall by exactly 1 AND the addressed region must be gone; a wider drop comes back as collateral damage, never as success. Undo restores.

Parameters:

  - `region_name` (string)
  - `start_bar` (integer)
  - `track_name` (string) **(required)**

#### `logic_remove_silence`

Cut the silence out of ONE audio region — the first move of every audio-post session, and what other DAWs call strip silence. Logic Pro 12.3.1 has no command by that name: the real one is 'Remove Silence from Audio Region…' and it opens a floating window with a LIVE PREVIEW of how many regions the current settings would leave. That preview is why apply defaults to FALSE: the first call opens the window, reads Logic's own count and the current threshold/time settings, closes it again and changes nothing, so an agent can ask 'what would this do?' before doing it. apply: true presses OK and verifies against the arrangement map (one region becomes N; Undo restores it, and the audio FILE is never touched). Refuses on a MIDI region. NOT IMPLEMENTED: the window's four numeric fields (threshold in dB, minimum silence, pre-attack, post-release) are per-digit steppers and this server does not write them — the result reports their current values, and changing them means touching Logic's window. MAY RETURN `warning` — read it before the rest of the result (RESULT CONTRACT in the server instructions).

Parameters:

  - `apply` (boolean): false (default) previews and changes nothing; true commits.
  - `region_name` (string)
  - `start_bar` (integer): The region's current start bar.
  - `track_name` (string) **(required)**

#### `logic_select_regions`

Select MANY regions at once — the thing logic_select_region deliberately cannot do (it is exclusive and single). Modes, each one a real Logic command: 'track' (every region on the anchor's track), 'following' (the anchor and everything after it, on EVERY track), 'following_same_track' (the anchor and everything after it on that track only), 'all' (every region in the project), 'none' (clear the selection). The relative modes need an anchor: track_name, plus region_name and/or start_bar when the track holds more than one region — the anchor is selected exclusively first, then the command extends from it. VERIFICATION: the number of selected regions is counted before and after off the arrangement map, and a mode that moved nothing comes back success: false rather than pretending. The count sees VISIBLE track rows only, while the selection itself is project-wide — a following edit acts on every selected region, counted or not. Uses learned key commands (Logic 12.3.1 names: 'Select All Regions/Cells of Same Track', 'Select All Following', 'Select All Following of Same Track/Pitch', 'Select All', 'Deselect All'). THIS CAN WRITE INTO THE USER'S OWN LOGIC: a command missing from the registry is LEARNED on the spot, which adds a MIDI-note assignment to the user's active key command set (additive, removable in Logic's Key Commands window), and the result then carries `learned_key_command`, `learned_note` and a `consent_note` saying exactly that. Say so when you report the result.

Parameters:

  - `mode` (string) **(required)**: Which selection command to fire. One of: `track`, `following`, `following_same_track`, `all`, `none`.
  - `region_name` (string): Which region is the anchor (with start_bar to disambiguate).
  - `start_bar` (integer): The anchor region's current start bar.
  - `track_name` (string): The anchor's track; required for every mode except 'all' and 'none'.

#### `logic_split_region`

Split ONE region at a bar (and optional beat) - the three-call recipe (select, park the playhead, fire 'Split Regions/Events at Playhead Position') as a single call with one verdict. Three failure modes are named and checked in order, and the first two refuse BEFORE anything is written: (1) the split point is not inside the named region - refused with the region's own bar span, nothing moved; (2) the playhead did not land where it was asked to - this parks the sub-beat fields of the control bar's position display as well, which logic_set_playhead does not, because a 'verified' bar/beat can still sit almost a whole beat late and for a split that is a wrong cut rather than a rounding error; (3) the command fired and the arrangement map still shows one region - reported as verification_failed with nothing undone. Success is proven by the map: two regions where one was, both reported. A MIDI SPLIT IS NOT SILENT: when a note crosses the cut, Logic raises a modal ('Notes Crossing Split Point') and freezes until it is answered - this answers it with notes_crossing (default 'split', Logic's own pre-selection) and reports which branch it took, and it cancels any dialog left over on a failure path, because an unanswered modal makes every later tool report 'the command fired and nothing happened'. Undo restores the single region. The halves are NEW regions - re-read logic_list_regions before addressing either of them. MAY RETURN `warning` — read it before the rest of the result (RESULT CONTRACT in the server instructions).

Parameters:

  - `at_bar` (integer) **(required)**: Bar to cut at; must be inside the region.
  - `at_beat` (integer): Beat within that bar, 1-based. Default 1 (the bar line).
  - `notes_crossing` (string): What happens to a MIDI note that straddles the cut, when Logic asks: 'keep' (the note stays whole in the first region), 'shorten' (truncated at the cut), 'split' (cut in two - Logic's own pre-selection and the default here). Audio regions and cuts no note crosses raise no dialog at all, and the result then says notes_crossing: 'not_asked'. One of: `keep`, `shorten`, `split`.
  - `region_name` (string): Which region; with start_bar, to disambiguate.
  - `start_bar` (integer): The region's CURRENT start bar.
  - `track_name` (string) **(required)**

#### `logic_move_region`

Move one region by whole bars and/or beats via Logic's nudge key commands (no dragging, no mouse). Whole-bar moves are verified exactly against the arrangement map. DESTRUCTIVE: a nudged region can land ON TOP of its neighbours, and Logic trims whatever it overlays - the region that was there loses the overlapped part and only Undo brings it back. Read logic_list_regions first to see what is in the way. Relative, so a repeat moves again.

Parameters:

  - `by_bars` (integer): Positive = right, negative = left.
  - `by_beats` (integer)
  - `region_name` (string)
  - `start_bar` (integer): Which region (its current start bar).
  - `track_name` (string) **(required)**

#### `logic_copy_region`

Copy (or move, with move: true = Cut) one region to a target bar, optionally onto another track: exclusive select, Copy/Cut, select destination track, park the playhead, Paste. Verified by the region appearing at the target bar in the arrangement map. IT LANDS ON THE BAR LINE, and that is not free: Paste lands at the playhead EXACTLY, while the control bar's position display publishes whole bars and beats only - measured, a 'verified' park sat a third of a beat late every time and a marker fired there came out a beat off. So the playhead is rewound and stepped onto the grid, and the sub-beat position is read back off the control surface; a park that cannot be proven exact refuses BEFORE Paste rather than pasting inside the beat. It also establishes Logic's Tracks-area keyboard focus first, because Cut/Copy/Paste act on the focused area and a copy fired without it does nothing at all, silently - and when nothing lands the refusal names what the focus actually was and reads Logic's window list, instead of sending you hunting for a modal that is not open. DESTRUCTIVE: the paste can land ON TOP of a region already at the target bar, and Logic trims what it overlays; move: true CUTS the source instead of copying it, so the original is gone from where it was. Only Undo puts either back - read logic_list_regions first.

Parameters:

  - `move` (boolean): true uses Cut instead of Copy (moves across tracks).
  - `region_name` (string)
  - `start_bar` (integer)
  - `to_bar` (integer) **(required)**
  - `to_track` (string): Destination track; default same track.
  - `track_name` (string) **(required)**

#### `logic_get_region_params`

Read a region's own parameters out of Logic's Region inspector — the panel at the top of the left inspector that says 'Region: <name>'. This is the read side of logic_set_region_params and the only way to see a region's quantize, transpose, velocity, loop, mute, gain, fades or delay. Pass track_name (plus region_name and/or start_bar) and the region is selected first; call it with no arguments to read whatever is selected. THREE THINGS THE RESULT TELLS YOU BEFORE THE VALUES. `subject` says whose parameters these are: a region, 'multiple' when several are selected (values that differ read as mixed), or 'defaults' — with NOTHING selected the panel shows the TRACK's region defaults ('MIDI Defaults' / 'Audio Defaults'), which is a different thing entirely and is never written by this server. `region_type` is read off the rows Logic published, independently of the arrangement map: a MIDI region has Velocity Offset, Dynamics, Gate Time and the Q-rows, an audio region has Gain, Fine Tune, Fade-In/Out, Reverse and Smart Tempo. And `enabled` per row is load-bearing: Logic greys out every Q-row while Quantize is Off, and a disabled control cannot be written. `display` is Logic's own text for the value and is ABSENT at a parameter's default, because Logic prints the default blank. The panel is opened (and its 'More' section with it) and put back exactly as it was found.

Parameters:

  - `include_quantize_values` (boolean): Also open the Quantize pop-up and return every value Logic offers (note values, triplets, swing A-F, tuplets). Costs a menu open; the menu is always dismissed.
  - `region_name` (string): With track_name: which region.
  - `start_bar` (integer): With track_name: the region's current start bar.
  - `track_name` (string): Select this track's region first. Omit to read whatever is selected.

#### `logic_set_region_params`

Set a region's own parameters through Logic's Region inspector. MIDI: quantize (with its swing and strength), transpose, velocity offset, dynamics, gate time, delay. AUDIO: gain, fine tune, transpose, fade-in and fade-out with their curves and the fade/crossfade type, reverse, delay. Both: loop and mute. These are Logic's NON-DESTRUCTIVE playback parameters — no notes are rewritten and no audio file is touched, so every one of them is reversible by setting it back, and logic_list_events keeps showing the recorded positions rather than the quantized ones. Pass as many as you like in one call; they are applied in a fixed order with QUANTIZE FIRST, because Logic disables every Q-row while Quantize is Off (so 'quantize to 1/16 with 75% swing' is one call, not two), and each fade LENGTH before its curve and type. Each write is read back off Logic's own control and reported as before/after with the row it landed on; a parameter already at the requested value is a verified no-op in `unchanged` and nothing is pressed (with every named parameter already right, the whole call comes back as state: "already_set"). Compare-and-set with `expected_current` per parameter. VALUES: units and ranges are on each property below, and an out-of-range number is REFUSED rather than clamped. Four parameters take Logic's OWN MENU SPELLING — quantize, dynamics, gate_time, fade_type — and a near miss is refused with the real list rather than guessed at; read the quantize list with logic_get_region_params include_quantize_values. SCOPE: the default 'region' selects the named region exclusively and writes to it alone. scope 'selection' writes to every region currently selected (set that up with logic_select_regions) — measured, not assumed: two selected regions both took the write — and it leaves the selection alone. A parameter whose value DIFFERS between the selected regions reads as mixed and cannot be compare-and-set. REFUSED, on purpose: with nothing selected the panel shows the track's region defaults and a write there would change what every future region inherits; a MIDI-only parameter on an audio region (or the reverse) is refused by name BEFORE anything is written; under scope 'selection' every numeric parameter is refused, because Logic makes those controls relative over a multi-selection; and a fade row whose label pop-up has been switched to 'Speed Up'/'Slow Down' is refused, because its value is then a ramp length and not a fade.

Parameters:

  - `delay_ticks` (integer): Playback offset in ticks; 240 ticks = a 1/16 note. Logic shows it as a musical value ('-1/32').
  - `dynamics` (string): Velocity scaling by name: Fixed, 25%, 50%, 75%, 88%, 94%, 100%, 106%, 112%, 125%, 150%, 175%, 200%, 300%, 400%. MIDI regions only.
  - `expected_current` (object): Compare-and-set: the same parameter names with the values you believe are current. Any mismatch refuses with precondition_failed and writes nothing.
  - `fade_in_curve` (integer): Shape of the fade-in, -99..99 (0 is linear). Audio regions only.
  - `fade_in_ms` (integer): Fade-in length in milliseconds. Audio regions only. 0 removes the fade.
  - `fade_out_curve` (integer): Shape of the fade-out, -99..99 (0 is linear). Audio regions only. Logic labels this row 'Curve' exactly like the fade-in's; it is addressed by position.
  - `fade_out_ms` (integer): Fade-out length in milliseconds. Audio regions only. 0 removes the fade.
  - `fade_type` (string): Fade-out/crossfade type, Logic's own menu spelling: 'Out', 'X (Crossfade)', 'EqP (Equal Power Crossfade)', 'X S (S-Curved Crossfade)'. The short form alone ('X', 'EqP', 'X S') is accepted, case-insensitively. Audio regions only.
  - `fine_tune` (integer): Cents, on top of transpose. Audio regions only.
  - `gain_db` (number): Region gain in DECIBELS, one decimal (-6.5, +3.0). Audio regions only. Logic holds it as tenths of a dB; the result reports the raw value, the dB and Logic's own display.
  - `gate_time` (string): Note-length scaling by name: the Dynamics list plus 'Legato'. MIDI regions only.
  - `loop` (boolean): Loop the region to the next region or the project end.
  - `mute` (boolean): Mute this region (not the track).
  - `q_strength` (integer): How far notes are pulled to the grid, 0-100 %. 100 is Logic's default; lower it to quantize and still leave feel. Needs quantize to be on.
  - `q_swing` (integer): Swing percentage; 50 is straight. Needs quantize to be on.
  - `quantize` (string): Logic's own menu spelling, e.g. 'Off', '1/16 Note', '1/8 Swing C'.
  - `region_name` (string): Which region, with start_bar to disambiguate.
  - `reverse` (boolean): Play the region backwards. Audio regions only, non-destructive, and the file on disk is untouched.
  - `scope` (string): 'region' (default) selects the named region and writes only to it; 'selection' writes to every currently selected region and changes no selection. One of: `region`, `selection`.
  - `start_bar` (integer): The region's CURRENT start bar.
  - `track_name` (string): Which track the region is on. Required for scope 'region'.
  - `transpose` (integer): Semitones. Audio regions cap at ±36.
  - `velocity_offset` (integer): Added to every note's velocity. MIDI regions only.

#### `logic_rename_region`

Rename ONE region. Logic's Region inspector publishes the region's name as an editable text field, so this is a single write and a confirm — no dialog, no key command, and the inspector's disclosure triangles are not even touched. The region is selected exclusively first (track_name plus region_name and/or start_bar; ambiguity is refused with the candidates listed) and the rename is verified TWICE: the inspector reads the new name back, and the arrangement map shows it on the region at that position. Names are the ARRANGEMENT's, not the audio file's — renaming an audio region never touches the file on disk, and Undo restores the old name. Compare-and-set with expected_current_name. Two notes worth knowing: a MUTED region reads as '<name>, muted' in the arrangement map while the inspector shows the bare name, and this tool compares the bare names; and if Logic renumbers OTHER regions on the track as a side effect (the way it renumbers default marker names by position), the ones that moved are reported in `also_renamed`. To rename a TRACK use logic_rename_track; to rename several regions call this once per region.

Parameters:

  - `expected_current_name` (string): Compare-and-set: the name you believe the region carries. A mismatch refuses and writes nothing.
  - `new_name` (string) **(required)**: The new name. One line, non-empty.
  - `region_name` (string): Which region (its current name), with start_bar to disambiguate.
  - `start_bar` (integer): The region's current start bar.
  - `track_name` (string) **(required)**: Which track the region is on.

#### `logic_set_tempo`

Set the project tempo in BPM via the control bar's tempo display (rapid-fire stepwise converge, ~1.3 s per 120 BPM of distance). Whole-BPM resolution. Compare-and-set with expected_current_bpm. TEMPO MAP GUARD: the tempo display shows and sets the tempo AT THE PLAYHEAD, so on a project with a tempo track this write would edit one tempo node rather than the project tempo. It therefore reads the project's tempo map out of Logic's Tempo List (View > List Editors > Tempo; ~2 s, no playhead movement) and REFUSES with precondition_failed when the map holds more than one tempo: a tempo map is edited in Logic's tempo track / Tempo List, not through this slider. When the Tempo List cannot be read it falls back to sampling the tempo at the playhead and at bar 1 (parking the playhead and restoring it — roughly 0.13 s per bar of travel) and refuses the same way; two agreeing samples are evidence, not proof, so the result then reports which bars were compared in tempo_sampled_at_bars. There is no override argument. MAY RETURN `warning` — read it before the rest of the result (RESULT CONTRACT in the server instructions).

Parameters:

  - `bpm` (number) **(required)**: Target tempo, 5-990.
  - `expected_current_bpm` (number): Abort unless the current tempo matches.

#### `logic_save_project`

Save the open Logic project — the ONLY way this server ever saves; no other tool saves as a side effect. Fires the Save key command and verifies via the document's modified flag. Refuses when more than one project is open, when the project has never been saved, or when expected_project_path does not match. Returns state: "already_saved" when there is nothing to save - a verified no-op, not a failure.

Parameters:

  - `expected_project_path` (string): Recommended: absolute .logicx path that must match the open project.

#### `logic_new_project`

Create a NEW Logic project at the given .logicx path — dialog-free, from a bundled empty project template — and open it. Logic runs single-project: if the current project has unsaved changes the call fails unless if_current_modified explicitly chooses 'save' or 'dont_save'. The created project is already saved on disk.

Parameters:

  - `if_current_modified` (string): 'fail' (default), 'save' or 'dont_save' — what to do with the currently open project's unsaved changes. One of: `fail`, `save`, `dont_save`.
  - `path` (string) **(required)**: Absolute destination path ending in .logicx; must not already exist.

#### `logic_open_project`

Open an existing .logicx project. Single-project semantics as logic_new_project: unsaved changes in the current project require an explicit if_current_modified decision.

Parameters:

  - `if_current_modified` (string): 'fail' (default), 'save' or 'dont_save'. One of: `fail`, `save`, `dont_save`.
  - `path` (string) **(required)**: Absolute path to an existing .logicx.

#### `logic_duplicate_project`

Duplicate the OPEN project on disk and (by default) open the copy — the safe sandbox for destructive experiments. Default destination: '<name> Copy.logicx' next to the original; a destination_path whose folder does not exist yet is created. The copy is a same-volume APFS clone, so a 174 MB project costs ~20 ms and no extra disk space (measured); a destination on ANOTHER volume copies real bytes. WHAT HAPPENS TO THE ORIGINAL, in words, because this tool is sold on the answer: the copy is the project's ON-DISK state, so unsaved changes are not in it unless you pass save_first: true — which SAVES THE ORIGINAL to get them there. Opening the copy closes the original (single-project mode), and Logic then asks what to do with any unsaved changes: if_current_modified defaults to 'fail', so a modified original is REFUSED with nothing copied and nothing closed rather than written to disk behind you — 'save' writes the original and leaves those changes OUT of the copy, 'dont_save' throws them away, save_first: true puts them in both, and open_copy: false leaves the original open and untouched. Note that Logic marks a project modified as soon as it is opened, so 'dont_save' is the honest answer for a project you have not actually edited. The result says which happened: `saved_before_copy`, `original_written_to_disk`, `original_unsaved_changes_discarded` and `dialogs_answered`. The open is verified by PATH against Logic's own document list — never by the project's name, which cannot tell two projects with the same basename apart. If the copy is made and the open then fails, the error names the copy's path and says so, so the retry does not hit 'already exists'. MAY RETURN `warning` — read it before the rest of the result (RESULT CONTRACT in the server instructions).

Parameters:

  - `destination_path` (string): Optional .logicx path for the copy. Missing parent folders are created. Default: '<name> Copy.logicx' beside the original.
  - `if_current_modified` (string): What happens to the ORIGINAL's unsaved changes when the copy is opened: 'fail' (default) refuses before anything is copied, 'save' writes them to the original (they will NOT be in the copy), 'dont_save' discards them. Same values and same default as logic_open_project. One of: `fail`, `save`, `dont_save`.
  - `open_copy` (boolean): Open the copy after duplicating, which closes the original. Default true. False leaves the original open and makes this a pure disk copy.
  - `save_first` (boolean): Save the open project before copying so the copy includes unsaved changes — this WRITES THE ORIGINAL to disk. Default false.

#### `logic_close_project`

Close the open project and prove it closed. 'saving' must be an explicit 'yes' or 'no' — there is no default, because discarding versus persisting changes is always the caller's decision. **If you mean to close one project and open another, call logic_reset_to (clean slate, same path or a different one) or logic_open_project instead — both fold the close in, so closing first is a wasted round-trip.** This tool is for leaving Logic with nothing open. It shares logic_reset_to's close: the AppleScript runs off-thread (Logic's AppleScript suite blocks while a modal is up) while an Accessibility loop walks whatever Logic puts on screen, and every dialog it saw is reported in `dialogs`. With saving:'no' it answers 'Do you want to save the changes…?' with **Don't Save** — that is the contract you chose. With saving:'yes' that same alert is REPORTED AND NEVER PRESSED, along with any dialog whose grammar it does not know, and the call then fails on its timeout with the alert's own text and buttons rather than clicking a button whose consequence was never measured. The close is polled, not slept on, and `verified` comes from Logic's document list actually answering that the project is gone — a readback that could not be read is never reported as a successful close. All four per-project caches (bank map, tempo map, meter map, plugin parameter names) are cleared, listed in `caches_cleared`: they are scoped by project path, which cannot tell a reopened SAME path from state measured against tracks that only existed unsaved. expected_project_path is checked BEFORE anything closes, and a never-saved project (no path to compare) is refused rather than closed unguarded. MAY RETURN `warning` — read it before the rest of the result (RESULT CONTRACT in the server instructions).

Parameters:

  - `expected_project_path` (string): Absolute path of the project you believe is open. Checked against Logic's own document path before anything closes, and the close is refused on a mismatch — the guard against closing the wrong project after Logic switched under you. A project that has never been saved has no path to compare, so passing this refuses that close instead of skipping the check.
  - `saving` (string) **(required)**: 'yes' saves before closing; 'no' discards unsaved changes. One of: `yes`, `no`.
  - `timeout_seconds` (number): How long to wait for the close, polling Logic while it happens (5-300, default 30).

#### `logic_reset_to`

**THIS TOOL DISCARDS THE OPEN PROJECT'S UNSAVED CHANGES. THAT IS ITS CONTRACT, NOT A SIDE EFFECT.** The episode-reset primitive: close whatever is open WITHOUT saving, open the .logicx at `path`, and prove the world is in a known state. Built for eval harnesses and for any agent that wants a clean slate between attempts — reset to the same file it was already in and every experiment of the last episode is gone. `confirm_discard: true` is REQUIRED and has no default, so this cannot be tripped into; call logic_save_project first if the changes matter. Refuses BEFORE touching Logic when the target file does not exist, so a bad path never costs you the open project. IT OWNS THE DIALOGS: the close runs off-thread (Logic's AppleScript blocks while a modal is up) while an Accessibility loop walks whatever appears, answers 'Do you want to save the changes…?' with **Don't Save** and the auto-save recovery prompt with **Saved**, and reports every dialog it saw in `dialogs`. A dialog whose grammar it does not recognise is REPORTED AND NEVER PRESSED — the reset then fails on the timeout with the alert's own text and buttons in the log, rather than clicking a button whose consequence was never measured. CACHES: all four per-project caches (bank map, tempo map, meter map, plugin parameter names) are cleared explicitly while nothing is open. They are scoped by project path, which already covers switching to a DIFFERENT project — but reopening the SAME path keeps the scope token identical, so a bank map measured against tracks that only existed unsaved would otherwise survive the reset and be trusted. VERIFICATION: the frontmost document window matches the target path, exactly one document is open, it is unmodified, logic_health's Accessibility and process checks pass, and no dialog is left on screen. Every check is reported individually and `verified` is the AND of all of them. If the open fails after the close, the error says so explicitly — the previous project is already gone at that point and Logic has nothing open. MAY RETURN `warning` — read it before the rest of the result (RESULT CONTRACT in the server instructions).

Parameters:

  - `confirm_discard` (boolean) **(required)**: Must be exactly true. Acknowledges that the open project's unsaved changes are thrown away. There is no default.
  - `path` (string) **(required)**: Absolute path to the .logicx to reset to. Must already exist; pass the SAME path the project is currently at to reset an episode in place.
  - `timeout_seconds` (number): How long to wait for the close (5-300, default 30). The open has its own 30 s budget.

#### `logic_project_snapshot`

The TRUTH DOCUMENT: one call that aggregates the existing readers into a structured, diffable picture of the project. Pair it with logic_reset_to to diff an episode's start and end state, or call it once to understand a project you did not build instead of making twenty reads. SCOPE decides the cost, not the capability — each level is a superset of the one before. 'structure' (default) is Accessibility-only and never touches the control surface: transport (tempo, meter, playhead, project path), the tempo map, the meter map, markers, the rendered track list and the region map. 'mix' adds the control-surface strip census and the full mixer snapshot (two bank walks — the expensive part). 'full' adds per-track MCU inserts and sends, one strip selection each, capped by max_tracks. COMPLETENESS IS THE CONTRACT: every section named in `sections` is present in the result, and one that could not be read comes back as {"unavailable": "<reason>"} — never a missing key, because a diff would read a missing section as an empty project rather than a failed reader. `complete` is false whenever any section is unavailable, and `unavailable_sections` names them. Deterministic by design: fixed section and array ordering, keys serialized sorted, so two snapshots of the same project diff cleanly. `timing_ms` carries the per-section cost and is the ONE nondeterministic block — drop it before diffing. Composes the same functions the individual tools call (logic_get_transport, logic_list_signatures' meter map, logic_markers, logic_list_tracks, logic_list_regions, logic_list_strips, logic_mixer_snapshot, logic_list_inserts route 'mcu', logic_mcu_sends), so their caveats apply here unchanged — in particular the track list is only the RENDERED rows, and the MCU sections degrade honestly against an old bridge daemon. MAY RETURN `warning` — read it before the rest of the result (RESULT CONTRACT in the server instructions).

Parameters:

  - `max_tracks` (integer): Cap on tracks walked by scope 'full', default 8. Exceeding it truncates the inserts/sends sections, which say so.
  - `scope` (string): 'structure' (default, AX-only), 'mix' (+ census and mixer), 'full' (+ per-track inserts and sends). One of: `structure`, `mix`, `full`.

#### `logic_setup_key_commands`

One-time onboarding: learn MIDI-note assignments for all standard key commands (Toggle Track Freeze, Undo, Redo, Flashback Capture as Recording, Split at Playhead, Create Marker) into the user's Logic via the Key Commands window automation. Additive to the user's key command set and removable there; collisions with existing assignments get alternate notes automatically. Idempotent — already-learned commands are verified and skipped, each reported in `results` as state: "already_learned" (the same entries also carry the older key `status`, which will be dropped in a later release). Runs automatically the first time a tool needs a missing command, so calling this explicitly is optional. Pass relearn: true to force re-learning even for commands that look bound — the repair when key commands silently stopped firing (e.g. after the MIDI ports were recreated: Logic scopes the assignments to the port identity).

Parameters:

  - `commands` (array of string): Limit to these standard command names (default: all).
  - `relearn` (boolean): Force re-learning of every standard command even when an assignment is already shown. Repairs bindings orphaned by MIDI-port changes. Default false.

#### `logic_learn_key_command`

Learn ANY command in Logic's Key Commands window - not just the standard set - onto a MIDI note, so logic_trigger_key_command can fire it. Give the command's name EXACTLY as the Key Commands window spells it (e.g. 'Strip Silence…', 'Bounce Regions in Place', 'Select All Following of Same Track'); the search field is driven with the first words of that name unless you pass 'search' yourself. THIS WRITES INTO THE USER'S OWN LOGIC KEY COMMAND SET: the command gains an additional assignment on the dedicated 'Logic MCP Commands' MIDI port. It is additive - the user's existing keyboard shortcut is untouched - and removable in the same window (select the command, Delete Assignment). The MIDI note is chosen automatically from a range reserved for learned commands (60-99, then 122-127, then 21-59), so it can never take a note the product's own standard commands want; pass 'note' only to force one. The registry file is the consent record and records the name, note, timestamp, search term and that THIS tool bound it - read it back with logic_list_key_commands. Already-registered commands answer immediately without opening the window, as state: "already_registered" (or "already_learned" when the window confirmed an existing assignment); pass relearn: true to bind again, the repair after MIDI ports were recreated. When no row matches, the failure is not_found and it LISTS the rows the panel was showing: command names drift between Logic versions, so a near miss is answered with the real spellings rather than a guess. MAY RETURN `warning` — read it before the rest of the result (RESULT CONTRACT in the server instructions).

Parameters:

  - `dry_run` (boolean): Look, do not bind: open the window, filter on 'search', return every command name it shows with the assignment it already carries, and close again. Use this FIRST when unsure of a name — nothing is written to the user's key command set.
  - `name` (string) **(required)**: The command name exactly as Logic's Key Commands window shows it. Case-insensitive; Logic's own spelling is what gets registered.
  - `note` (integer): Force a specific MIDI note instead of the next free one. Refused when another registered command already holds it.
  - `relearn` (boolean): Bind again even when the registry already lists this command, wiping its existing controller assignments first. Default false.
  - `search` (string): Optional search term for the window's filter field (default: the first words of 'name'). Use a shorter term when the exact name is uncertain - the not_found answer then lists more near misses.

#### `logic_list_key_commands`

List what the key command registry holds: every command name that has been learned onto the 'Logic MCP Commands' MIDI port, its note and channel, when it was learned, and which tool bound it. Read-only and Logic-free - this reads the registry FILE, so an entry it lists can still have been orphaned inside Logic (recreated MIDI ports do that silently; logic_setup_key_commands with relearn: true repairs it). The registry is what logic_trigger_key_command checks before firing anything, so this is also the list of commands an agent may fire. Also reports which standard commands are not learned yet.

Parameters:

  - (no parameters)

#### `logic_trigger_key_command`

Fire a Logic key command that was learned onto the dedicated 'Logic MCP Commands' MIDI port. Pass name (e.g. 'Toggle Track Freeze', 'Undo') or note+channel. Standard commands missing from the registry are learned automatically first; unknown notes are refused because they could be bound to anything. CAUTION with Undo: the menu shows no operation name, so only fire it right after a known edit.

Parameters:

  - `channel` (integer): MIDI channel, default 16.
  - `name` (string): Registered command name, e.g. 'Toggle Track Freeze'.
  - `note` (integer): MIDI note of a registered command.

#### `logic_render_track`

Render ONE track offline to an audio file ON DISK with ZERO dialogs, via Track Freeze. NOT bounce-in-place: it produces a FILE, it does not commit a new audio region into the project, so it is not the tool for 'print that so I can chop it' - resampling a part back into the arrangement has no route in this server yet. Mechanism: selects the track, toggles freeze over the 'Logic MCP Commands' MIDI port, presses play (Logic then renders the whole track offline, typically seconds), copies the 32-bit float AIFF out of Media/Freeze Files to the captures folder, and unfreezes again. Requires 'Toggle Track Freeze' in the key command registry and the MCU bridge running. WHAT `verified` MEANS HERE: it reports the CLEANUP, not the render. `verified` aliases `unfrozen`, so a perfectly good render whose freeze could not be undone comes back verified: false with the track still frozen in Logic (unfreeze it there). Judge the render itself by `path` and `metrics`. Renders the full track from project start including all plugins and automation (freeze mode Pre Fader). If the track is already frozen the call fails safely and restores state. TEMPO: with start_bar/end_bar the slice's boundaries are integrated over the project's tempo map, read out of Logic's Tempo List (View > List Editors > Tempo; ~2 s, no playhead movement, cached per project and reported in tempo_map). When the Tempo List cannot be read the slice falls back to constant-tempo bar math and the tempo is sampled at both ends of the range instead (parks the playhead, reads, restores), with a `warning` naming both readings when they differ — the FULL render is unaffected either way. Without a bar range no tempo is read at all. MAY RETURN `warning` — read it before the rest of the result (RESULT CONTRACT in the server instructions).

Parameters:

  - `beats_per_bar` (number): Override meter; default reads the control bar's time signature.
  - `blind` (boolean): LISTEN FIRST: withhold this result's measurements of the audio — peak/RMS metrics and, for an A/B, the dB deltas — so the only thing left to describe the sound from is the sound. The audio blocks, every file path, `success`, `verified`, `state` and any `warning` all still come back untouched, and the withheld keys are sealed into the JSON file named by `sealed_metrics_path`, which you open AFTER writing down what you heard (nothing is re-rendered). Default false. If you can receive audio, pass true on your FIRST listen of any material — see LISTENING in the server instructions.
  - `end_bar` (integer): Exclusive: the slice ends where this bar begins. Must be greater than start_bar.
  - `expected_project_path` (string): Absolute .logicx path; when given, the open project's AXDocument must match before anything is changed.
  - `include_audio` (boolean): Attach the rendered audio as MCP audio content blocks, default true — see AUDIO RESULTS in the server instructions before passing false. Either way the result carries a resource_link to logician://captures/<filename>, so false still leaves you able to fetch the audio with resources/read.
  - `label` (string): Filename label; default is derived from the track name.
  - `start_bar` (integer): With end_bar: also cut this bar range out of the render as a separate 32-bit float WAV with its own metrics (bar 1 = project start).
  - `tempo` (number): Override BPM for the bar math; default reads the control bar. Only used when the tempo map cannot be read from the Tempo List — a readable map is integrated instead. Constant METER is still assumed (signature changes are not read).
  - `track_name` (string) **(required)**: Track to render, matched against MCU LCD names or AX track headers.
  - `track_number` (integer): Optional AX row number to disambiguate duplicates.

#### `logic_mcu_command`

Send a command to Logic through the Mackie Control bridge (UI-independent). cmd is one of: press {button: play|stop|record|rewind|forward|cycle|click|bank_left|bank_right|channel_left|channel_right|flip|name_value|assign_track|assign_send|assign_pan|assign_plugin|assign_eq|assign_instrument|...}, select/mute/solo {channel: 0-7}, fader {channel: 0-8, value: 0-16383, verify: true}, vpot {index: 0-7, delta: +-n}, vpot_press {index}, raw {bytes: [..]}, ping. Read logic_mcu_status afterwards to verify via Logic's feedback. FADER: Logic does follow an absolute fader write (measured, with and without the fader-touch note), but it SNAPS the position to its own resolution — 5631 through 5635 all landed on 5628 — so never compare the echo to the value you asked for with ==. Pass verify: true to get final_value (where Logic actually settled) and followed back; to restore a fader exactly, write back a value Logic itself reported, which is on its grid by construction. Channel 8 is the dedicated master fader, which is Logic's `Master` strip and NOT `Stereo Out`.

Parameters:

  - `button` (string): One of: `assign_eq`, `assign_instrument`, `assign_pan`, `assign_plugin`, `assign_send`, `assign_track`, `bank_left`, `bank_right`, `channel_left`, `channel_right`, `click`, `cycle`, `drop`, `flip`, `forward`, `global_view`, `marker`, `name_value`, `nudge`, `play`, `record`, `replace`, `rewind`, `smpte_beats`, `solo_global`, `stop`.
  - `bytes` (array of integer)
  - `channel` (integer)
  - `cmd` (string) **(required)**: One of: `await`, `converge`, `fader`, `keycmd`, `midi_abort`, `midi_stream`, `mute`, `ping`, `press`, `raw`, `select`, `solo`, `status`, `vpot`, `vpot_press`.
  - `delta` (integer): Vpot ticks, positive = clockwise; magnitudes above 63 are clamped.
  - `index` (integer): Vpot/channel-strip index 0-7.
  - `note` (integer)
  - `value` (integer): 14-bit fader value 0-16383.

#### `logic_get_audio_clip`

LISTEN to rendered audio: returns a short clip (default 8 s, max 20 s) of a local audio file as an MCP audio content block (mono AAC, roughly 64 KB per 8 s) that multimodal models can hear. Use this on the file paths returned by logic_render_track / logic_bounce_range / logic_evaluate_change. NEVER read raw audio files with a text/file tool - megabytes of binary will overflow the model context and can crash the client. Also writes the clip to disk (clip_path in the result): if the audio block does NOT reach you, your client drops MCP audio - open clip_path with your client's file viewer instead, which most clients pass to the model as real audio.

Parameters:

  - `duration_seconds` (number): Clip length, default 8, max 20.
  - `include_audio` (boolean): Attach the rendered audio as MCP audio content blocks, default true — see AUDIO RESULTS in the server instructions before passing false. Either way the result carries a resource_link to logician://captures/<filename>, so false still leaves you able to fetch the audio with resources/read.
  - `path` (string) **(required)**: Absolute path to a local audio file (AIFF/WAV/etc).
  - `start_seconds` (number): Offset into the file, default 0.

#### `logic_get_transport`

Read the transport state and where the playhead is right now, from the control bar: playing, recording, cycle, playhead position in bars/beats, tempo, time signature, key signature, metronome, count-in. Read-only. Fields whose control bar element is missing are null. The Smart Tempo project tempo mode (Keep/Adapt/Auto) is NOT in the cheap read — Logic publishes no value on the control bar's Project Tempo pop-up — so it comes back as project_tempo_mode_note saying why. Pass read_smart_tempo_mode: true to get the real value: that opens File > Project Settings on the Smart Tempo pane, reads the mode off its pop-up and closes the window again (~1.6 s, nothing written), and the result then carries project_tempo_mode plus project_tempo_mode_route. Worth knowing before recording: an ADAPT-mode project rewrites its own tempo map to follow a recording, and logic_record_midi runs this same read and refuses on Adapt/Auto.

Parameters:

  - `read_smart_tempo_mode` (boolean): Also read the Smart Tempo project tempo mode from File > Project Settings (opens and closes that window; default false, which keeps this call a pure control-bar read with no side effect).

#### `logic_tempo_events`

Read and WRITE the project's tempo map — the tempo track, as rows in Logic's Tempo List. This is what makes a tempo map buildable: 'land a downbeat on the hit at 1:12' is create-an-event, and until now the map could only be read. action 'list' returns every event (bar, beat, BPM) and writes nothing; the map is cached per project, every cache hit is cross-checked against the control bar's live tempo, and a list whose cross-check could NOT run (the control bar unreadable, e.g. a non-English Logic UI) comes back verified: false with read_route 'tempo_list_cache' and a warning - never as a verified live read. 'create' makes a new event at bar/beat with bpm; 'set' retunes the event already there; 'delete' removes it. HOW THE WRITE WORKS, because two parts of it are surprising. Logic's own 'Create new Tempo Event' button places the event AT THE PLAYHEAD, not on a bar line, and parking the playhead only sets its bar and beat — the division and tick it already carried come along — so this tool creates the event and then steps the new row's own position fields back to the exact bar/beat you asked for. And the BPM is written through the CONTROL BAR's tempo slider, which edits the tempo event in force at the playhead: with the playhead parked on the event, that is this event. Neither half is trusted: the whole map is re-read afterwards and the result reports it, so a create that produced two events, or moved a neighbour, comes back as a failure (or a warning naming which other events moved) rather than as a success. Compare-and-set with expected_current_bpm on 'set' and 'delete'. REFUSED: 'create' where an event already sits (use 'set'), 'set'/'delete' where none does, deleting bar 1 (the project's base tempo — retune it instead), and any write at all while the Tempo List cannot be read, because a tempo write is not made blind. COST: a few seconds, dominated by the playhead travel to the bar (~0.13 s per bar) and by the tempo slider's one-BPM-per-step convergence. AFTER A WRITE the server's cached tempo and meter maps are dropped, so every later bar->seconds conversion re-reads the new map. MAY RETURN `warning` — read it before the rest of the result (RESULT CONTRACT in the server instructions).

Parameters:

  - `action` (string): 'list' (default) reads the map and writes nothing. One of: `list`, `create`, `set`, `delete`.
  - `bar` (integer): Which bar the event sits on. Required for create/set/delete.
  - `beat` (integer): Which beat within the bar (default 1).
  - `bpm` (number): The tempo. Required for create and set. Logic's slider holds whole BPM.
  - `expected_current_bpm` (number): Compare-and-set for 'set' and 'delete': the BPM you believe the event carries. A mismatch refuses and writes nothing.

#### `logic_set_cycle`

Turn cycle (loop) mode on or off via the control bar Cycle button and verify the new state.

Parameters:

  - `enabled` (boolean) **(required)**

#### `logic_set_playing`

Start or stop playback via the control bar Play button and verify the new state. Starting plays from the current playhead position (or the cycle range when cycle is on).

Parameters:

  - `playing` (boolean) **(required)**

#### `logic_set_playhead`

Move the playhead to a 1-based bar (and optional beat) by stepping the control bar position display, then verify. Requires the control bar display mode that exposes bar/beat (Beats & Project).

Parameters:

  - `bar` (integer) **(required)**
  - `beat` (integer)

#### `logic_set_cycle_range`

Set the cycle (loop) locators to a whole-bar range, e.g. bars 5-9. Anchors the ruler's grid-snapped cycle region to a bar line via the playhead thumb, moves the region start by writing its AXPosition, adjusts the length by dragging its right edge (hit-test guarded), verifies via the region's bar-denominated size description, and restores the playhead. The target range must be visible in the ruler. Optionally turns cycle on/off afterwards via 'enabled'.

Parameters:

  - `enabled` (boolean): When given, turn cycle mode on or off after setting the range.
  - `end_bar` (integer) **(required)**: Bar where the cycle ends (exclusive right locator, as shown in Logic).
  - `start_bar` (integer) **(required)**: 1-based bar where the cycle starts.

#### `logic_select_track`

Select a track by name (and optional 1-based track number) so its channel strip is exposed in the inspector. Writes AXSelectedChildren on the Tracks header group, falls back to the header's Has Focus button, and verifies through both the header's selected state and the inspector strip. Fails with ambiguous when several visible tracks share the name, and restores the previous selection if verification fails. Only tracks whose headers are currently rendered can be selected.

Parameters:

  - `expected_project_path` (string): Absolute .logicx path; when given, the open project's AXDocument must match before anything is changed.
  - `track_name` (string) **(required)**: Exact track name as shown in the track header.
  - `track_number` (integer): 1-based track number; required when several visible tracks share the name.

#### `logic_set_track_stack`

Expand or collapse a track stack by pressing its disclosure triangle, verifying the new state, and reporting which subtracks were revealed or hidden. Fails with not_exposed if the track is not a stack. Subtracks of a collapsed stack are otherwise invisible to logic_list_tracks and logic_select_track.

Parameters:

  - `expanded` (boolean) **(required)**: true to show the stack's subtracks, false to hide them.
  - `expected_project_path` (string): Absolute .logicx path; when given, the open project's AXDocument must match before anything is changed.
  - `track_name` (string) **(required)**: Exact name of the stack's main track.
  - `track_number` (integer): 1-based track number; required when several visible tracks share the name.

#### `logic_survey_plugins`

Inventory every insert on a track: open each plugin window, list its accessible parameters (name, raw range, writability), classify the exposure, and close windows that were opened. Takes a few seconds per insert. Use to map which plugins are controllable through this MCP. ACCESSIBILITY-ONLY: a headerless strip works only while an inspector is SHOWING it — see STRIP ADDRESSING in the server instructions.

Parameters:

  - `track_name` (string) **(required)**
  - `track_number` (integer)

#### `logic_add_plugin`

Add a plugin to a track's first empty insert slot — mouse-free via the Mackie Control plugin browser (vpot-stepped, LCD-verified, vpot-press instantiates). Works for every plugin in Logic's browser including third-party. If the MCU bridge is down, the AX chooser fallback requires allow_mouse: true because it briefly takes over the pointer. `cross_check` names the SECOND, independent source: "ax_insert_list" (the inspector strip's insert list agreed) or "unavailable" (no inspector is showing that strip, so the control surface's own echo is the only evidence - the result warns when that happens). Accepts headerless output/aux/bus strips — see STRIP ADDRESSING in the server instructions. MAY RETURN `warning` — read it before the rest of the result (RESULT CONTRACT in the server instructions).

Parameters:

  - `allow_mouse` (boolean): Permit the Accessibility chooser fallback, which moves the pointer. Default false (data-driven MCU browser only).
  - `expected_project_path` (string): Refuse unless this is the open project.
  - `format` (string): Channel format submenu item when offered, default 'Stereo'.
  - `plugin_name` (string) **(required)**: Menu title of the plugin, e.g. 'Gain', 'Channel EQ', 'Decapitator'.
  - `track_name` (string) **(required)**
  - `track_number` (integer)

#### `logic_remove_plugin`

Remove a plugin from a track — mouse-free via the Mackie Control plugin browser's No Plug-in entry (can take up to ~60 s of vpot stepping; verified via LCD and an AX cross-check on the named track). When the same display name occupies several slots, the mouse-free route refuses to guess: pass insert_slot to name the one to remove. If the MCU bridge is down, the AX chooser fallback requires allow_mouse: true because it briefly takes over the pointer. `cross_check` names that second source: "ax_insert_list" (the inspector strip's insert list agreed the plugin is gone - one fewer instance, when several were there) or "unavailable" (no inspector is showing that strip, so the control surface's own echo is the only evidence - the result warns when that happens). Accepts headerless output/aux/bus strips — see STRIP ADDRESSING in the server instructions. MAY RETURN `warning` — read it before the rest of the result (RESULT CONTRACT in the server instructions).

Parameters:

  - `allow_mouse` (boolean): Permit the Accessibility chooser fallback, which moves the pointer. Default false (data-driven MCU browser only).
  - `expected_project_path` (string): Refuse unless this is the open project.
  - `insert_index` (integer): Same-plugin disambiguator for the allow_mouse Accessibility FALLBACK only (the mouse-free route takes insert_slot instead) - and DESTRUCTIVE if it is wrong: the plugin at that ordinal is the one removed. ACCESSIBILITY ordinal (logic_list_inserts route 'ax'), NOT the Mackie insert_slot — see INSERT NUMBERING in the server instructions.
  - `insert_slot` (integer): Needed only when the same plugin sits in several slots - names which one the mouse-free route removes. Its LCD cell must show plugin_name or the call refuses without pressing anything. MACKIE physical slot 1-8 (logic_list_inserts route 'mcu'), NOT the Accessibility insert_index — see INSERT NUMBERING in the server instructions.
  - `plugin_name` (string) **(required)**
  - `track_name` (string) **(required)**
  - `track_number` (integer)

#### `logic_set_track_mute`

Mute or unmute a track via its inspector channel strip mute button, verified by readback (control surface first, inspector strip as fallback). A track already in the requested state is a verified no-op (state: "already_on" / "already_off") and nothing is pressed. Accepts headerless output/aux/bus strips — see STRIP ADDRESSING in the server instructions.

Parameters:

  - `enabled` (boolean) **(required)**: true mutes the track, false unmutes it.
  - `expected_current` (boolean): Compare-and-set: the mute state you believe the track is in. A mismatch refuses with precondition_failed and presses nothing.
  - `track_name` (string) **(required)**
  - `track_number` (integer)

#### `logic_set_track_solo`

Solo or unsolo a track via its inspector channel strip solo button, verified by readback (control surface first, inspector strip as fallback). A track already in the requested state is a verified no-op (state: "already_on" / "already_off") and nothing is pressed. A solo left on silently empties every later bounce, so unsolo before judging a mix. Accepts headerless output/aux/bus strips — see STRIP ADDRESSING in the server instructions.

Parameters:

  - `enabled` (boolean) **(required)**: true solos the track, false unsolos it.
  - `expected_current` (boolean): Compare-and-set: the solo state you believe the track is in. A mismatch refuses with precondition_failed and presses nothing.
  - `track_name` (string) **(required)**
  - `track_number` (integer)

#### `logic_set_track_volume`

Set a track's volume fader to a dB value, converging against Logic's own dB readout and reporting before_db / after_db / requested_db. `db` is ABSOLUTE, never an offset - read the current value first (logic_track_info and logic_mixer_snapshot both report volume_db), or pass relative_db instead, which computes the target from the value read immediately before the write ('2 dB louder' is relative_db: 2). The two are mutually exclusive and one of them is required. Compare-and-set with expected_current_db. Fader steps are about 0.1-0.3 dB apart; default tolerance 0.15 dB. WHAT `verified` MEANS HERE: exactly what you asked for — the landed value is within tolerance_db of the target, with no widening. When the fader will not reach that, the control-surface route re-converges and, failing that, returns verified: false with `deviation_db` and a `verification_note` saying how far out it stopped. `success` stays true either way, because the fader did move and after_db is Logic's own dB readout of where it is. Accepts headerless output/aux/bus strips — see STRIP ADDRESSING in the server instructions.

Parameters:

  - `db` (number): ABSOLUTE target level in dB (e.g. -14.2, 0.0). Mutually exclusive with relative_db; one of the two is required.
  - `expected_current_db` (number): Compare-and-set: the dB you believe the fader is at. A difference of more than 0.5 dB refuses with precondition_failed and moves nothing.
  - `relative_db` (number): Offset in dB from the value read immediately before the write: 2 is '2 dB louder', -3 is '3 dB quieter'. Mutually exclusive with db. Saves the read-then-guess round trip, and the result still reports before_db and after_db.
  - `tolerance_db` (number): Accepted deviation from the target, default 0.15 dB. Note that `verified` on the control-surface fast path uses max(tolerance_db, 0.6) - see the description.
  - `track_name` (string) **(required)**
  - `track_number` (integer)

#### `logic_set_track_pan`

Set a track's pan/balance knob position via the inspector strip, verified by readback. `position` is ABSOLUTE, not an offset: read the current value first (logic_track_info or logic_mixer_snapshot both report `pan`) and pass the number you want it to end on. Reports `before` and `after`. Accepts headerless output/aux/bus strips — see STRIP ADDRESSING in the server instructions.

Parameters:

  - `expected_current_position` (integer): Compare-and-set: the position you believe the knob is at. A mismatch refuses with precondition_failed and writes nothing.
  - `position` (integer) **(required)**: ABSOLUTE knob position, typically -64..63 with 0 at center; the knob's own range is enforced. Out of range is refused, never clamped.
  - `track_name` (string) **(required)**
  - `track_number` (integer)

#### `logic_open_plugin`

Open the plugin window for one insert on the named (selected) track by pressing the insert's open button, then verify that the window appeared. If the window was already open it is identified via its toggle behaviour and restored. Fails closed on not_found, ambiguous (two inserts with the same plugin), not_exposed and verification_failed.

Parameters:

  - `expected_project_path` (string): Absolute .logicx path; when given, the open project's AXDocument must match before anything is pressed.
  - `insert_index` (integer): Required when the same plugin occupies several slots. ACCESSIBILITY ordinal (logic_list_inserts route 'ax'), NOT the Mackie insert_slot — see INSERT NUMBERING in the server instructions.
  - `plugin_name` (string) **(required)**: Plugin display name; truncated slot names such as 'Space D' match by prefix.
  - `track_name` (string) **(required)**

#### `logic_close_plugin`

Close the plugin window of one insert on the named (selected) track by toggling the insert's open button, verifying that a window titled after the track disappeared. Precise even when several plugin windows share the same title. Reads the window list BEFORE pressing: with no window open for that track it returns state already_closed in ~10 ms without pressing anything, because the open button is a toggle and pressing it would OPEN the plugin. In the rare case where the press opens a window instead of closing one (another plugin on the same track had the only window with that title), that window is closed again and precondition_failed is returned.

Parameters:

  - `insert_index` (integer): Required when the same plugin occupies several slots. ACCESSIBILITY ordinal (logic_list_inserts route 'ax'), NOT the Mackie insert_slot — see INSERT NUMBERING in the server instructions.
  - `plugin_name` (string) **(required)**
  - `track_name` (string) **(required)**

#### `logic_close_plugin_window`

Close one plugin window by pressing its close button and verifying that THAT window is gone (its element and its title, not merely some window). Closes only windows whose Accessibility subrole is AXDialog: plugin and auxiliary windows, INCLUDING document-carrying ones such as Drum Machine Designer. Any other subrole is refused - the project window and the Mixer are AXStandardWindow - and the refusal names the subrole it found. Fails with ambiguous when several windows share the title; use logic_close_plugin there. A window still on screen after the press is reported success: false, verified: false, never as closed.

Parameters:

  - `window_title` (string) **(required)**

#### `logic_list_plugin_parameters`

Read a plugin's parameter names and current formatted values. ONE tool, TWO routes, and `route_used` says which ran. Pass window_title for the ACCESSIBILITY route: it reads an already-open plugin window and adds `ax_writable` per parameter — whether logic_set_plugin_parameter can write it through a text field. Logic reads a plugin through its sliders and writes it through editable "knob and field" controls, and knob-only plugins publish sliders but no field at all (measured: Channel EQ 26 sliders / 0 fields, Limiter 4 / 0), so an all-false list means the plugin is read-only from that plane. Pass track_name + insert_slot for the CONTROL-SURFACE route: host automation reads every MCU page, needs no window, and works for plugins whose UI exposes nothing to Accessibility (Decapitator, Trilian, ...) — it selects the strip and enters plugin edit mode. That route is PAGE-CAPPED: `truncated: true` with `pages` and `pages_total` means parameters were left unread — raise max_pages or treat the list as partial, never as the plugin's whole parameter set. Accepts headerless output/aux/bus strips — see STRIP ADDRESSING in the server instructions.

Parameters:

  - `insert_slot` (integer): Names the plugin on the control-surface route. MACKIE physical slot 1-8 (logic_list_inserts route 'mcu'), NOT the Accessibility insert_index — see INSERT NUMBERING in the server instructions.
  - `max_pages` (integer): Control-surface route only: page cap, default 12 (each uncached page costs ~1.7 s; large instruments have 80+). pages_total and truncated report what was left out.
  - `route` (string): 'auto' (default) picks by argument: window_title means ax, track_name + insert_slot means mcu, and both means ax with an mcu fallback. 'ax' or 'mcu' forces one and refuses rather than falling back. One of: `auto`, `ax`, `mcu`.
  - `track_name` (string): Control-surface route: the strip the plugin sits on. With insert_slot, this is the route that needs no window.
  - `track_number` (integer): Tracks only; disambiguates two headers sharing a name.
  - `window_title` (string): Accessibility route: exact Logic plugin window title, usually the track name. The window must already be open (logic_open_plugin).

#### `logic_list_events`

Read the MIDI events of a region out of Logic's Event List (View > List Editors > Event) — position, type, pitch, velocity and length, as Logic's own cells print them. This closes the asymmetry where logic_record_midi could WRITE MIDI that nothing could read back. SCOPE, and it matters: the Event List shows the SELECTED region (or the selected track's region at the playhead), never the project's MIDI as a whole — pass track_name (plus region_name and/or start_bar) to select one first, or select with logic_select_region and call this with no arguments to read whatever is showing. An EMPTY list means nothing is selected, not that the project has no MIDI, and the result says so. Every row carries Logic's published columns verbatim plus parsed bar/beat/pitch/velocity/length where the columns were recognised. The row count is cross-checked against the list's own 'Number of Items' and a mismatch REFUSES rather than returning a truncated take on the region (an AX table publishes only realised rows). Opens the List Editors pane if it was closed, restores the previously selected tab, and closes what it opened. MAY RETURN `warning` — read it before the rest of the result (RESULT CONTRACT in the server instructions).

Parameters:

  - `limit` (integer): Maximum events in the result, default 500. The full count is always reported as event_count.
  - `region_name` (string): With track_name: which region.
  - `start_bar` (integer): With track_name: the region's current start bar.
  - `track_name` (string): Select this track's region first (exclusive selection). Omit to read whatever is currently selected.

#### `logic_edit_event`

Fix ONE MIDI note in place — its pitch, velocity, position or length — or delete it, or add one, through Logic's Event List. This is the SURGICAL editor, not a composer: logic_record_midi plays a whole part in and is the tool for writing music, while this one is for the single flubbed note in a take you want to keep. SCOPE: the Event List edits the SELECTED region — pass track_name (plus region_name and/or start_bar) to select one first, or select with logic_select_region — and read the region first with logic_list_events, because that is where the addresses come from. ADDRESSING: bar plus pitch, because a chord publishes several rows on the same position; add beat/division/tick to narrow further. An address that matches two events REFUSES with both listed rather than editing the nearer one. MOVING a note: any to_* field you leave out keeps its current value, so to_beat alone moves the note without quantizing its sub-beat feel. Compare-and-set with expected_current_velocity and expected_current_length; an edit that asks for what is already there is a verified no-op (state 'already_set') and presses nothing. VERIFICATION, every time: the list is re-read and must show the event count the action implies, the event reading exactly what was asked, and EVERY OTHER EVENT UNTOUCHED — a write that disturbed a neighbour comes back with a warning naming which. WORTH KNOWING: every cell is a one-step-per-write stepper, so a big velocity or pitch move costs a few seconds; the table RE-SORTS on every position and pitch write, so row numbers from an earlier read are stale; and Logic's 'Create new Event' places the note at the PLAYHEAD, which this tool parks and then corrects. REFUSED: writing while the Event List is showing the project's REGIONS instead of a region's events (nothing there is editable); a row that is not a Note, because Logic's Num/Val columns mean controller number and value there; creating a duplicate of a note already at that position and pitch, which nothing could address afterwards; and creating OUTSIDE the selected region's bars, because Logic adds the note and does not grow the region, leaving an event that exists and never sounds. NOT writable from this plane at all: the Status column (an event's type) and the MIDI channel. MAY RETURN `warning` — read it before the rest of the result (RESULT CONTRACT in the server instructions).

Parameters:

  - `action` (string) **(required)**: 'set' edits an existing note, 'create' adds one, 'delete' removes one. One of: `set`, `create`, `delete`.
  - `bar` (integer) **(required)**: The event's bar (for 'create', the bar to put it on). Required.
  - `beat` (integer): The event's beat within the bar. Narrows the address; for 'create' it is where the note goes (default 1).
  - `division` (integer): Third position field. Narrows the address; for 'create', default 1.
  - `expected_current_length` (string): Compare-and-set on the length, in the same four-field spelling.
  - `expected_current_velocity` (integer): Compare-and-set: the velocity you believe the note carries. A mismatch refuses and writes nothing.
  - `length` (string): The new length in Logic's own 'bars beats divisions ticks' spelling — the text logic_list_events prints. '0 1 0 0' is a quarter note.
  - `new_pitch` (string or integer): Transpose the addressed note to this pitch. Same spelling as pitch.
  - `pitch` (string or integer): For 'set'/'delete': WHICH note at that position, which is how a chord is told apart. For 'create': the note to make. A MIDI number 0-127 or Logic's own name, where C3 is middle C (60): 'D#2', 'A♯2', 'C3'.
  - `region_name` (string): With track_name: which region.
  - `start_bar` (integer): With track_name: the region's current start bar.
  - `tick` (integer): Fourth position field. Narrows the address; for 'create', default 1.
  - `to_bar` (integer): Move the note to this bar. Omitted position fields keep their current value.
  - `to_beat` (integer): Move the note to this beat.
  - `to_division` (integer): Move the note to this division.
  - `to_tick` (integer): Move the note to this tick.
  - `track_name` (string): Select this track's region first (exclusive). Omit to edit whatever region is showing.
  - `velocity` (integer): The new velocity (1-127).

#### `logic_markers`

Markers: list, create, goto, rename or delete them. 'list' reads Logic's Marker List (View > List Editors > Marker) with each marker's bar and name. 'create' fires Logic's own Create Marker key command at the PLAYHEAD (pass bar to park the playhead there first) and verifies against a fresh read of the list; note that Logic's position stepping lands inside the bar rather than exactly on its line, so a marker can sit a fraction of a beat late. 'goto' parks the playhead at a named marker's bar. 'delete' uses the list row's own Delete action and verifies the marker is gone (Undo restores it). 'rename' writes the row's name cell IF Logic publishes a settable one and REFUSES with the reason if not — the Tempo List's cells turned out to be steppers rather than fields, so this is checked at runtime rather than assumed. Address a marker by name (exact, case-insensitive — never fuzzy, because renaming or deleting the wrong marker is silent damage) or by bar; ambiguity refuses with the candidates listed. MAY RETURN `warning` — read it before the rest of the result (RESULT CONTRACT in the server instructions).

Parameters:

  - `action` (string): Default 'list'. One of: `list`, `create`, `goto`, `rename`, `delete`.
  - `bar` (integer): Which marker (its bar), for goto/rename/delete. For 'create': park the playhead at this bar first.
  - `name` (string): Which marker (exact name), for goto/rename/delete. For 'create': the name to give it, applied as a separate write that is reported separately.
  - `new_name` (string): Required for action 'rename'.

#### `logic_list_signatures`

Read the project's time signatures out of Logic's Signature List (View > List Editors > Signature): each signature, the bar it starts on, and its bar length in QUARTER-note beats (what Logic's BPM counts — 6/8 is three beats a bar, 7/8 three and a half). This is the meter map, the last assumption that was left in this server's bar math after the tempo map landed. HOW IT IS USED: a map with more than one bar length is INTEGRATED by every tool that converts bars to seconds itself (logic_render_track's slice, logic_evaluate_change method 'render', logic_record_midi's note placement and verification slice, logic_record_automation's point placement) — those results then carry a meter_map block and a warning, and an explicit beats_per_bar argument no longer overrides the project's own grid. A map with ONE bar length is reported and deliberately not used, so a constant-meter project's boundaries are bit-for-bit what they have always been. The Signature List also holds KEY signatures; those rows are counted for the truncation cross-check and skipped. Read cost ~2 s, no playhead movement, cached per project.

Parameters:

  - (no parameters)

#### `logic_set_insert_bypass`

Bypass or un-bypass one insert — the fastest honest A/B in mixing, and the write side of the bypass state logic_list_inserts has always been able to READ. Address the insert by plugin_name, by insert_index, or both (both is safest: a name that does not match the slot at that index is refused). insert_index is the ACCESSIBILITY ordinal from logic_list_inserts, NOT the Mackie insert_slot the logic_mcu_* tools take. Compare-and-set with expected_current_bypassed; an insert already in the requested state is a verified no-op (state: "already_bypassed" / "already_active") rather than a blind toggle, because this control publishes only AXPress and no absolute write. Verified by re-reading the same checkbox. Pass expected_current_bypassed anyway; it is what turns a stale idea of the state into a refusal instead of a wrong toggle. ACCESSIBILITY-ONLY: a headerless strip works only while an inspector is SHOWING it — see STRIP ADDRESSING in the server instructions.

Parameters:

  - `bypassed` (boolean) **(required)**: true bypasses the plugin, false makes it active again.
  - `expected_current_bypassed` (boolean): Abort with precondition_failed unless the insert is currently in this state.
  - `insert_index` (integer): 1-based. ACCESSIBILITY ordinal (logic_list_inserts route 'ax'), NOT the Mackie insert_slot — see INSERT NUMBERING in the server instructions.
  - `plugin_name` (string): Plugin display name as logic_list_inserts shows it; truncated names such as 'Space D' match by prefix.
  - `track_name` (string) **(required)**
  - `track_number` (integer): Disambiguates duplicate track names; tracks only.

#### `logic_set_mixer`

WINDOW MANAGEMENT: opens or closes the Mixer WINDOW (Window > Open Mixer), verified against the window list. It does not set any mixer VALUE - faders, pans, mutes and sends are logic_set_track_volume, logic_set_track_pan, logic_set_track_mute/solo and logic_mcu_set_send, none of which need this window. Returns state already_open / already_closed when the window is already the way you asked for. WHAT THE MIXER DOES AND DOES NOT DO (measured, not hoped): the Mixer window publishes EVERY channel strip to Accessibility - the result lists them in `mixer_strips`, Master, Stereo Out and the auxes included - but they are NOT inspector strips, and the Accessibility strip tools (logic_list_inserts, logic_survey_plugins, logic_open_plugin, logic_plugin_preset, logic_set_insert_bypass) still cannot address them: with the Mixer open, logic_list_inserts on Master fails exactly as it does with the Mixer closed. `inspector_strips` is the list those tools CAN reach (the selected track and its output). For Master, an aux or a bus, use the logic_mcu_* tools, which never needed a window. AND IT COSTS SOMETHING: the Mixer is a second document window carrying the same project, so it can shadow the project window - while Logic is in the background it may be the only window Accessibility publishes, and then every track-header read fails. This server skips Mixer windows when it resolves the project window, but close the Mixer when you are done anyway. What it is genuinely good for: putting the Mixer in front of a HUMAN, and reading the complete strip census out of the window. MAY RETURN `warning` — read it before the rest of the result (RESULT CONTRACT in the server instructions).

Parameters:

  - `open` (boolean) **(required)**: true opens the Mixer, false closes it.

#### `logic_set_plugin_parameter`

Set ONE plugin parameter, verified by readback. START HERE: `{track_name, plugin_name, parameter, target_value}` is the whole call — "more bass around 500 Hz" is logic_set_plugin_parameter {track_name: "Bas", plugin_name: "Channel EQ", parameter: "Pea2Ga", target_value: "4.9"}, with no logic_list_inserts and no logic_list_plugin_parameters in front of it. The tool finds the insert itself from the strip's own insert list and reports which one it used in `resolved_slot`; pass insert_slot instead when you already know the physical slot, or to pick between two copies of the same plugin. ONE tool, TWO routes, and `route_used` says which ran. track_name + plugin_name (or insert_slot) is the CONTROL-SURFACE route: host automation (MCU vpot) with the LCD value echo as readback — it reaches EVERY plugin, custom-UI third-party included, needs no window, converges numeric targets adaptively, steps text targets (e.g. 'On', 'B') until exact match, matches the parameter against the MCU's abbreviated names ('Thrs' matches 'Threshold'), and rolls back on failed verification; expected_current_value is optional there. Pass window_title instead for the ACCESSIBILITY route: the parameter's formatted text field is written and read back, and the prior value is restored on verification failure; it needs expected_current_value (compare-and-set) and only reaches parameters logic_list_plugin_parameters marks `ax_writable`. Give BOTH and a knob-only plugin no longer dead-ends: the write starts on the Accessibility route and falls back to the surface when the parameter publishes no field, reporting route_used 'mcu' and fallback_from 'ax'. Accepts headerless output/aux/bus strips — see STRIP ADDRESSING in the server instructions.

Parameters:

  - `expected_current_value` (string): Compare-and-set. REQUIRED on the ax route (it is that route's whole safety contract); optional on the mcu route, which verifies by LCD echo.
  - `insert_slot` (integer): Names the plugin on the control-surface route by physical slot, when you already know it or need to pick between two copies of the same plugin. plugin_name is the cheaper way in and costs no extra call. MACKIE physical slot 1-8 (logic_list_inserts route 'mcu'), NOT the Accessibility insert_index — see INSERT NUMBERING in the server instructions.
  - `parameter` (string) **(required)**: Parameter name as the plugin spells it - an EQ band's Frequency or Gain, a compressor's Threshold or Ratio, a reverb's Mix.
  - `plugin_name` (string): Control-surface route, and the one-call way in: name the plugin ('Channel EQ', 'Compressor') and the tool resolves the insert slot itself from the strip's insert list, reporting it as resolved_slot. Matched against the LCD's 6-character abbreviation, so 'Channel EQ' finds 'Cha EQ'. Two inserts answering to the name is reported as ambiguous with the slot numbers, never guessed — pass insert_slot to choose.
  - `route` (string): 'auto' (default) picks by argument: window_title means ax (falling back to mcu when the surface arguments are there and the parameter is not ax_writable), track_name + plugin_name/insert_slot means mcu. 'ax' or 'mcu' forces one and refuses rather than falling back. One of: `auto`, `ax`, `mcu`.
  - `target_value` (string) **(required)**: Value to write, in the parameter's own units - '500 Hz', '2.0 dB', 'On'.
  - `tolerance` (number): Control-surface route only: accepted deviation for a numeric target, in the parameter's own units.
  - `track_name` (string): Control-surface route: the strip the plugin sits on.
  - `track_number` (integer): Tracks only; disambiguates two headers sharing a name.
  - `window_title` (string): Accessibility route: exact plugin window title, usually the track name. The window must already be open (logic_open_plugin).

#### `logic_list_strips`

The CENSUS: every channel strip the control surface can reach, in project order — audio and instrument tracks, auxes, buses, the output and the master. Unlike logic_list_tracks, which can only see the track headers Logic has currently RENDERED (19 of 25 strips on the reference project), this walks the surface's banks, so nothing is hidden by scrolling or by a collapsed stack. Each row carries the strip's position, its bank/channel address and Logic's own 6-character LCD name cell; track_name and track_number are filled in only where exactly one rendered track header abbreviates to that cell, and everything else is reported as kind 'unresolved' (an output/aux/bus, a track scrolled out, or two tracks that share a name) rather than guessed at. Address strips by their full Mixer name, never by the abbreviation. Always walks the surface fresh — a census that could be stale would be the very failure this tool exists to fix — which costs a few seconds and refreshes the bank map every other control-surface tool uses.

Parameters:

  - (no parameters)

#### `logic_mixer_snapshot`

The whole mixer in ONE call, off Logic's own control-surface feedback: per strip the fader value in dB, pan, mute/solo/select/record-arm state and the raw 14-bit fader echo. volume_db is the dB string Logic paints in its channel-strip Volume view — the same readout logic_set_track_volume converges against — NOT a conversion of the fader position, which is reported separately and raw as fader_14bit; a cell that does not parse comes back null with the LCD text beside it, never interpolated. record_armed is sampled across a full blink cycle because Logic FLASHES an armed strip's record LED (~640 ms on / 640 ms off, measured), so a single instant would report half the armed strips as unarmed. Two bank walks (names and pan, then dB and LEDs), about 16 s on a 25-strip project; the surface is left in the pan view at the leftmost bank. Strip identity is established by a fresh scan, never from the cached bank map, and follows the same track_name/'unresolved' rule as logic_list_strips. METERS: where the bridge daemon publishes it, each strip also carries meter_level (0-12) and meter_overload — Logic's OWN control-surface meter, the segment count it would light on a Mackie Control. That is a state read of a value Logic published, exactly like the fader echo; it is NOT an audio measurement, has no dB calibration, and must never be reported as loudness. MEASURED: Logic does NOT feed this virtual surface meters during playback in the default Control Surfaces configuration — 8 s of rolling transport through real audio produced zero meter events (a handful of 0xD0 bytes arrive sporadically at idle, all level 0). Expect meter_level to be 0/absent until a way to enable Logic's MCU meter mode is found (candidate: the Mackie Control device's settings in Control Surfaces Setup — an open research item). Each bank is sampled at a different instant during the walk, so any values are eight-strip snapshots rather than a comparison across the mixer. When the running daemon predates this feature the fields are ABSENT rather than zero and meter_feed says 'unavailable'. MAY RETURN `warning` — read it before the rest of the result (RESULT CONTRACT in the server instructions).

Parameters:

  - (no parameters)

#### `logic_set_track_record_arm`

Arm or disarm a track for recording — the control surface's rec/ready button (MCU note 0x00-0x07), verified by Logic's own record LED AND, independently, by the track header's Record Enable checkbox. Compare-and-set: a track already in the requested state is reported as state: "already_armed" / "already_disarmed" and nothing is pressed. `cross_check` names the SECOND, independent source: "ax_record_enable_checkbox" (the track header agreed), "disagreed" (it did not - treat the arm state as unknown and re-read) or "unavailable" (no track header could be read, so the surface's LED is the only evidence). Logic FLASHES the record LED of an armed strip (~640 ms on / 640 ms off), so the LED evidence is a window rather than an instant: seen lit once means armed, and only a whole quiet window is read as disarmed. Several tracks can be armed at once, so arming one does NOT disarm another — read logic_mixer_snapshot before rolling if that matters. Output, aux, bus and master strips have no record enable and are refused before anything is pressed. Needs the MCU bridge: there is no Accessibility-only route. MAY RETURN `warning` — read it before the rest of the result (RESULT CONTRACT in the server instructions).

Parameters:

  - `enabled` (boolean) **(required)**: true arms the track, false disarms it.
  - `track_name` (string) **(required)**: Exact track name as Logic shows it (not the 6-character LCD abbreviation).
  - `track_number` (integer): 1-based track number; disambiguates duplicate track names.

#### `logic_set_metronome`

Turn the metronome click on or off via the control surface's click button, verified by reading the control bar's own Metronome Click checkbox back (the same field logic_get_transport reports), with the surface's click LED as a second source. Compare-and-set: already-correct is reported as state: "already_on" / "already_off" and nothing is pressed; a press that does not land is undone. Count-in is a separate setting and is not touched. MAY RETURN `warning` — read it before the rest of the result (RESULT CONTRACT in the server instructions).

Parameters:

  - `enabled` (boolean) **(required)**

#### `logic_load_instrument`

Load a software instrument into a track's INSTRUMENT slot — mouse-free via the control surface's instrument browser (the IN bank view's vpot), the slot logic_add_plugin cannot reach because it fills an insert instead. One vpot tick per browser entry, the shown entry re-verified after settling, and a vpot press instantiates; leaving the view cancels a browse without loading anything. Entries carry Logic's channel format ('Drum Kit Designer Stereo', 'Drum Kit Designer Multi-Output', 'Abbey Road Saturator (m) Mono'), so instrument may be given bare or with the format, or the format passed separately; matching is case-insensitive and exact on the name — a near miss is refused with the entries seen, never guessed at. Verified by the instrument slot's own name in the IN bank view; read the loaded instrument's parameters with logic_mcu_instrument_parameters as an independent second look. REPLACES any instrument already on the track, settings and all — Logic's Undo is the only way back. MAY RETURN `warning` — read it before the rest of the result (RESULT CONTRACT in the server instructions).

Parameters:

  - `expected_project_path` (string): Refuse unless this is the open project.
  - `format` (string): Channel format to pick when a plugin offers several: 'Stereo', 'Mono' or 'Multi-Output' (Logic also spells that last one 'Multi Output'). Matched case-insensitively against the suffix Logic puts on the browser entry. Default: the first entry whose name matches, and the result reports which format that was.
  - `instrument` (string) **(required)**: Instrument name as Logic's browser shows it, e.g. 'Drum Kit Designer', 'Sampler', 'Analog Lab V'. May include the format ('Drum Kit Designer Stereo').
  - `max_steps` (integer): How many browser entries to step through before giving up, default 1200 (~0.11 s each, so a full sweep of a large plug-in library can take a couple of minutes). The list holds every installed instrument in every channel format and is NOT alphabetical, so a 'never showed' refusal at a low cap usually means the browse had not reached it yet, not that the name is wrong.
  - `track_name` (string) **(required)**: Software-instrument track to load onto.

#### `logic_read_automation`

READ an existing automation curve before you overwrite it — volume, pan, a send level or any plugin parameter — over a bar range. NOTHING IN THE PROJECT CHANGES: no automation mode is switched, no fader or vpot is moved, and the playhead is put back where it started (`playhead_restored` says whether that worked). It is not free of side effects, though, which is why it is not flagged read-only: it SELECTS the strip it reads and leaves the control surface in its Pan view. Mechanism: park the playhead at each sampled position and read the value Logic chases the lane to, which is the verification pass logic_record_automation already runs, with the writing half removed. HONESTY: these are SAMPLES, not the lane's breakpoints — a move that happens entirely between two samples is invisible, so lower resolution_beats when the shape matters — and an unautomated lane reads as a flat line at the track's static value, which the result says rather than implying a curve exists. Positions run from start_bar beat 1 to end_bar beat 1 inclusive; if the grid would exceed max_points the step is widened rather than the range truncated. Costs roughly a second per sampled position. MAY RETURN `warning` — read it before the rest of the result (RESULT CONTRACT in the server instructions).

Parameters:

  - `end_bar` (integer) **(required)**: INCLUSIVE here, unlike the render and bounce ranges: the last sampled position is this bar's beat 1. Must be >= start_bar.
  - `insert_slot` (integer): Required when parameter is 'plugin'. MACKIE physical slot 1-8 (logic_list_inserts route 'mcu'), NOT the Accessibility insert_index — see INSERT NUMBERING in the server instructions.
  - `max_points` (integer): Cap on sampled positions, default 64. Exceeding it widens the step; the range is never truncated.
  - `parameter` (string): What to read. 'send' also needs send; 'plugin' also needs insert_slot and plugin_parameter. Default 'volume'. One of: `volume`, `pan`, `send`, `plugin`.
  - `plugin_parameter` (string): Parameter name as shown on the MCU, required when parameter is 'plugin'.
  - `resolution_beats` (integer): Beats between samples, default 1 (whole beats only — the playhead is parked on the beat grid).
  - `send` (integer): Send slot 1-8, required when parameter is 'send'.
  - `settle_seconds` (number): How long to let Logic's echo settle at each position, default 0.8 s. Raise if values look like the previous position's.
  - `start_bar` (integer) **(required)**
  - `track_name` (string) **(required)**

"""#
